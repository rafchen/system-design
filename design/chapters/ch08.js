const D = (name, o) => ["dec", { name, ...o }];
export default {id:"caching",num:"08",title:"Caching",example:"a product page on a shopping site",
tagline:"Which layer, which pattern, how wrong you can afford to be — every caching decision with its use-when, don't-use-when, cost and failure.",
terms:["CDN vs local vs distributed","Cache-aside","Write-through","Write-behind","TTL vs invalidation","Stampede","Negative caching","Hot key"],
objectives:["Pick the cache layer from how stale the data may be and how far it may travel","Pick cache-aside, read-through, write-through or write-behind per data type","Answer the six cache questions for any cache you draw","Design invalidation and state the staleness window it permits","Name and prevent stampedes, penetration, hot keys and cold starts"],
sections:[
{id:"layer",title:"Which layer: browser, CDN, local, distributed",blocks:[
["p","Ten million people load this product page a day. The description changes about once a month. The price changes a few times a year. The stock count changes every few seconds."],
["p","Three pieces of data on one page, three completely different tolerances for being wrong. That is the whole chapter."],
["p","A cache is a bet that the answer has not changed since you last asked. So the question is never whether to cache. It is how wrong you are willing to be, for how long, and about which field."],
["p","Each layer sits closer to the user, answers faster, and is harder to correct. Redis is one delete away from being right. Each application server's in-process cache is only right again when its own copy expires, because there is no way to reach into fifty processes at once. The CDN is right again when you purge, and purging is slow and coarse. A browser that already has the page is right again when the user reloads."],
["p","So the rule that organises everything below: a datum may live only in layers whose correction speed matches how urgently it must be right."],
["p","The description can live everywhere, including the browser. The price can live in Redis and briefly at the CDN. The stock count at the moment of checkout may live nowhere at all — that one read goes to the database every single time, because being wrong there costs a sale and an apology."],
D("CDN — a content delivery network holding copies close to users",{say:"Images and the product page itself are served from the content delivery network — a fleet of servers spread around the world that keep copies close to users. Copies expire after a short time, and while a copy is being refreshed the old one is still served rather than making anyone wait. Our own servers end up handling well under one percent of the traffic.",
use:["Files that are the same for everyone: images, video, stylesheets, scripts.","Pages and API responses that do not change per user.","Users spread across the world, where the distance itself is the delay."],
avoid:["Anything personalised. You would need the logged-in user in the cache key, which gives every user their own copy and defeats the point.","Anything that must be correct within seconds, because you cannot recall these copies quickly."],
consider:["Removing a copy early — a purge — is slow and works in broad strokes. For content that changes occasionally, a short expiry is more reliable than trying to purge on demand.","The cache key must include every request header the response depends on. Miss one and a cached page built for one user gets served to another.","You pay per gigabyte sent, so the bill grows with traffic even though your servers are idle."],
fails:"Three ways. You deploy a change and users keep seeing the old page until the copies expire. You forget to vary the cache key on a header and one person's page is handed to somebody else. Or one file becomes wildly popular in one region — which is not actually a failure, because absorbing that is what the network is for."}),
D("In-process cache — each server's own private copy",{say:"Each web server keeps the few thousand most popular products in its own memory. Reads take microseconds because nothing leaves the machine — no network call at all. The trade is that each server holds its own copy, so for a few seconds they can disagree with each other.",
use:["A small set of data that is read constantly.","Absorbing a single overwhelmingly popular item, so the shared cache is asked for it once per server per expiry rather than on every request."],
avoid:["Anything that must look identical on every server at the same instant.","Large amounts of data, because this is per-server memory and you are paying for it once per machine."],
consider:["There is no way to clear these caches on demand. You have fifty processes, each holding its own copy, and no direct line to any of them. Either wait for the expiry or broadcast a message telling every server to drop the entry.","Because you cannot clear them, keep the expiry short — seconds rather than hours."]}),
D("Distributed cache — one shared store every server reads (Redis, Memcached)",{say:"I would keep product details in Redis, a separate store that holds everything in memory and that every web server can reach. A request checks Redis first and only falls through to the database when Redis does not have the answer. Entries expire after an hour, and I delete the entry whenever someone edits that product, so the database only ever sees misses and writes.",
use:["Data that many people read and almost nobody changes.","Data where being slightly out of date does no harm.","Login sessions, feeds, rate-limit counters, and pages you assembled earlier and want to reuse."],
avoid:["Storing anything here that exists nowhere else. This is a copy, not your records. Redis can be configured to survive a restart, but if you are relying on that, say so deliberately rather than drifting into it.","Data that has to be exactly right on every single read — a bank balance, or stock at the moment of checkout. Read those from the database every time."],
consider:["Every server sees the same value, which is the whole advantage over each server keeping its own copy.","A read takes well under a millisecond, because it is one network hop to a machine holding the data in memory.","You have added a system that can break. If it goes down, every read arrives at the database at once, so decide now whether the database could survive that. If it could not, this is not really a cache — it is a dependency you have not been treating like one."],
fails:"Four ways, each covered later in this chapter. It is empty after a restart, so everything goes to the database at once. A popular entry expires and thousands of requests miss in the same instant. One extremely popular entry overloads whichever machine holds it. And a slow read can write a stale value back into the cache after somebody has already changed it."}),
["t","CDN → local → distributed → DB buffer pool: each step back is slower and more consistent. A price change reaches Redis with one delete, local caches at TTL, the CDN on purge, the browser on reload."]
]},
{id:"pattern",title:"Which pattern: aside, read-through, write-through, write-behind",blocks:[
["p","Cache-aside is the default, and it is worth being able to state in one breath: look in the cache; on a miss, read the database, write the answer into the cache with a TTL, and return it."],
["p","It is simple. It survives the cache vanishing, because everything simply becomes a miss. And it caches only what somebody actually asked for, rather than everything that might be asked for."],
["p","It also contains a race that almost nobody mentions and everybody eventually meets."],
["p","Request A reads the database and gets the old price. Before A can write it into the cache, an admin changes the price and deletes the cache entry. Then A writes — putting the old price into the cache with a fresh, full-length TTL. The delete happened before the write it was supposed to invalidate, and the stale value now outlives the change by an hour."],
["p","You can shorten the TTL so the damage is bounded, delete a second time a few hundred milliseconds after the write, or use a compare-and-set that only stores if nothing changed underneath. Which mitigation you choose matters much less than knowing the race is there."],
["p","Write-through takes the other approach: write to the cache and the database together on the write path, so the cache is never stale for anything you wrote. It costs write latency, and note what it does not fix — a bulk price import that goes straight to the database bypasses the write path entirely, so you still need a TTL underneath as a backstop."],
["p","Write-behind writes to the cache and flushes to the database later. That is how the view counter works: incrementing a database row once per page view would make it the single hottest row in the system, and nobody has ever needed an exact view count. The flush interval is a durability window — lose the cache node and you lose that many seconds of counts. For a view counter that is fine. For an order it would be a catastrophe, which is why the pattern is chosen per field and not per system."],
["p","And that is the real reason to know all four. One page uses several at once. On this product page: the description is cache-aside at an hour; the price is cache-aside at five minutes with an explicit delete on change; the stock count is read-through at thirty seconds and uncached at checkout; the view count is write-behind flushed every ten seconds; and the images sit on the CDN for a year, because their filenames contain a content hash and a changed image is simply a different filename."],
D("Cache-aside: the application manages the cache",{say:"The application looks in the cache. On a miss it reads the database, stores the result in the cache with an expiry, and returns it. Only data somebody actually asked for gets cached, and how stale it can be is bounded by the expiry plus deleting the entry whenever the record is edited.",
use:["Ordinary read-heavy data. This is the default, and you should need a reason to use anything else.","Data where losing the whole cache is survivable, because everything simply refills as it is requested."],
avoid:["Data that must never be even slightly out of date. Do not cache it at all.","Situations where many different parts of the code read the same data, because each one has to implement this identically and eventually one will not."],
consider:["The logic lives in your application, which means every read path has to do the same thing in the same order.","There is a race worth knowing about. Request A reads the old value from the database. Before A can store it, an edit changes the record and deletes the cache entry. Then A writes the value it read — the old one — with a full-length expiry. The deletion happened before the write it was meant to cancel. Shorten the expiry to limit the damage, delete a second time a moment after the edit, or only store if nothing changed underneath."]}),
D("Read-through: the cache fetches misses itself",{say:"Instead of the application reading the database on a miss, the cache does it. Callers only ever talk to the cache, so the loading logic is written once instead of in every place that reads this data.",
use:["Many different callers need the same data and you want one implementation of how it is loaded."],
avoid:["Cases where the cache has no way to reach your data source, which is most caches unless the library supports it."],
consider:["The behaviour is the same as cache-aside. The difference is only where the code lives, and the cost is that the cache is now coupled to your database."]}),
D("Write-through: update the cache as you write",{say:"When a price changes, write it to the cache and the database together before returning. A read can then never be out of date for anything we wrote ourselves. Writes get a little slower, and the price is never wrong.",
use:["Data where the cache must not disagree with storage for anything written through this path.","Data that will be read immediately after it is written, so you want the cache already holding it."],
avoid:["Data written far more often than it is read, because you fill the cache with entries nobody will ever ask for."],
consider:["This only protects the writes that go through this code. A bulk import that writes straight to the database bypasses it entirely, so you still want an expiry underneath as a backstop."]}),
D("Write-behind: update the cache now, the database later",{say:"View counts are incremented in the cache and written to the database in a batch every ten seconds. Writes become extremely cheap and the database sees a hundred times less load. If the cache machine dies you lose up to ten seconds of counts, which for view counts is fine.",
use:["Very high write rates where losing a few seconds of data is genuinely acceptable: view counts, metrics, when-last-seen timestamps."],
avoid:["Orders, payments, or anything you would have to apologise for losing."],
consider:["How much you can lose is exactly the gap between flushes. Ten seconds between writes means up to ten seconds of data at risk.","If the process doing the flushing dies quietly, you lose data and nothing reports an error. Monitor it."]}),
["ex","Product page, placed\nProduct document   cache-aside · TTL 1 h · delete on edit\nStock count        read-through · TTL 30 s · checkout re-reads the DB\nPrice              cache-aside · TTL 5 min · delete on change (legal risk)\nView counter       write-behind · flush 10 s\nImages             CDN · TTL 1 y · content-hashed filenames"],
["t","Cache-aside is simple and survives cache loss; write-through buys freshness with write cost; write-behind buys write speed with durability. Mixing patterns per data type on one page is normal — say which and why."]
]},
{id:"six-questions",title:"The six questions and the eviction choice",blocks:[
["p","Every cache in your diagram should be able to answer six questions. Being asked one you have not considered is exactly how a cache stops being an optimisation and becomes an outage."],
["ex","For every cache you draw\n1 Key?          product:{id}:v3  (version so schema changes miss cleanly)\n2 Stored what?  the assembled response (most savings, hardest partial invalidation) or the row?\n3 TTL?          1 h description · 30 s stock · 5 min price\n4 Invalidated?  delete product:{id}:* on save; TTL is the backstop\n5 Unavailable?  fall through to DB — can it survive? if not, this isn't a cache, it's a dependency\n6 Stale OK?     description yes · price minutes · stock at checkout no"],
["p","The fifth question is the one people skip, and it is the one that matters most."],
["p","What happens when this cache is unavailable? If the answer is that everything falls through to the database and the database copes, then it is a cache. If the answer is that the database cannot possibly survive that, then it is not a cache — it is a dependency you have been failing to treat like one, without the replication, failover and capacity planning that any other dependency would have been given."],
["p","That distinction is worth making explicitly in an interview, because it reframes a component everyone assumes is optional as one that is load-bearing."],
D("TTL and invalidation — an expiry plus explicit deletion",{say:"Deleting the entry when the record changes keeps things fresh on the write paths I remembered. The expiry limits the damage on the ones I forgot. I always have both, because there is always one I forgot.",
use:["Every cache, without exception."],
avoid:["Relying only on an expiry for data that somebody may need to correct urgently.","Relying only on deletion, because one forgotten admin script or migration leaves a wrong value cached forever."],
consider:["Delete the entry rather than updating it. Updating means computing the new value in the write path, where it can be computed wrongly. Deleting means the next read rebuilds it from the source of truth, where it cannot.","If writes can come from several services, have the write publish an event that others react to by deleting. That catches writes from anywhere, at the cost of a few milliseconds of delay.","An alternative is to put a version number in the cache key. Increment the version and every old entry becomes unreachable at once, with no mass deletion — at the cost of one extra lookup to learn the current version."]}),
D("Eviction (LRU or LFU) — what to drop when the cache is full",{say:"First, size the cache so that it rarely has to throw anything away. Then evict whatever was used least recently, unless a one-off scan of the whole catalogue would push out everything popular — in which case evict by how often things are used instead.",
use:["Least-recently-used when recent popularity predicts future popularity, which is usually true.","Least-frequently-used when popularity is stable over long periods and you want to survive scans."],
avoid:["Treating eviction as your way of clearing stale data. Eviction removes whatever is unpopular, which has nothing to do with whatever is wrong."],
consider:["Least-recently-used is easily fooled. A crawler reading every product once touches everything recently, so it pushes out the genuinely popular items in favour of items nobody will ask for again.","Least-frequently-used is slow to notice when something newly popular arrives, because the old favourites still have higher historical counts.","Real caches rarely implement either exactly. They sample a handful of entries and evict the worst of those, which is close enough and far cheaper."]})
]},
{id:"failure-modes",title:"Stampedes, penetration, hot keys, cold starts",blocks:[
["p","Four ways caches fail. All four are invisible on a normal day and total on a bad one."],
["p","The first is a stampede. A popular entry expires. In the millisecond after it does, five thousand in-flight requests all miss, and all five thousand go to the database — which has been comfortably serving one query an hour for that row and has no idea what is about to happen."],
["p","Nothing failed. The entry expired, which is precisely what you asked it to do. The fix is to let one request perform the refill while the others wait a few milliseconds for its result, or to serve the stale value and refresh in the background. Both are a handful of lines, and neither is there by default."],
["p","The second is penetration. A request for an id that does not exist misses the cache every time, because there is nothing to store — so it reaches the database every time. Somebody scraping sequential ids therefore bypasses your cache completely, which is the exact opposite of what you built it for. The fix is to cache the absence, with a short TTL so that a genuinely new object does not appear missing for long."],
["p","The third is a hot key, which you already met on election night: one entry read so often that a single cache node saturates on network alone. Replicate that entry across several nodes, or let every application server hold it in memory after one miss."],
["p","The fourth is the one that catches teams during an incident rather than before it. You restart the cache cluster. It is empty. The database has not seen full production read traffic since the day the cache was introduced — possibly years ago — and has never once been provisioned for it. Sending full traffic at an empty cache is how a five-minute cache restart becomes a forty-minute outage."],
["p","Which is really the point of this chapter. A cache that is load-bearing needs the same care as a database: warming, failover, capacity planning, an owner. Calling it just a cache is how that care quietly never happens."],
D("Stampede: everyone misses at the same instant",{say:"When a very popular entry expires, I let one request rebuild it while the others wait a few milliseconds for that result — or serve the old value and refresh in the background. Either way, five thousand simultaneous misses do not become five thousand database queries.",
use:["Any entry that is read constantly and has an expiry, which is most of them."],
consider:["Have the first request take a short lock and rebuild while the rest wait for it. This is often called single-flight or request coalescing.","Or refresh slightly before the expiry, chosen at random per request, so the rebuild happens while the old value is still being served.","Or serve the stale value immediately and refresh behind the scenes, which is the same idea the content delivery network uses."],
fails:"A popular entry expires. Thousands of requests miss in the same millisecond. A database row that normally receives one query an hour receives thousands at once, and the database falls over — not because anything broke, but because the entry expired exactly as instructed."}),
D("Cache penetration — requests for things that do not exist",{say:"A request for an id that does not exist misses the cache every time, because there is nothing to store, so it reaches the database every time. I cache the fact that it is missing for thirty seconds, and put a filter in front that can cheaply reject ids that were never valid.",
use:["Any public id space that somebody can guess, enumerate or scrape.","Lookups for objects that have been deleted."],
consider:["Cache the absence for a short time only. If you cache it for an hour and the object is created five minutes later, it will appear missing for the remaining fifty-five.","A Bloom filter is a small structure that can say this id is definitely not one of ours, without any lookup at all. It occasionally says maybe when it means no, which costs you a wasted lookup but never a wrong answer."]}),
D("Hot key — one entry far more popular than the rest",{say:"The product on the home page is read a hundred thousand times a second, and all of those reads land on whichever single cache machine holds it. That machine runs out of network capacity long before it runs out of anything else. So I copy the entry onto several machines, and let each web server keep it in its own memory, which means the shared cache is asked for it once per server per expiry instead of on every request.",
use:["Any entry read often enough that one machine's network connection becomes the limit."],
consider:["Store the same entry under several keys and read from one at random, which spreads the load across machines.","Or keep it in each application server's own memory, which removes the network entirely for that entry.","This is the same problem as the hot partition in Chapter 07, and it has the same shape: spreading requests evenly does nothing when they all want the same thing."]}),
D("Cold start — an empty cache after a restart",{say:"A cache that has just started is empty, and the database has not handled full production read traffic since the day the cache was introduced — possibly years ago. So I load the most popular entries before the cache takes any traffic, and then send it traffic gradually rather than all at once.",
use:["Every deploy or restart of a cache the system depends on."],
avoid:["Pointing full production traffic at an empty cache and hoping. This is how a five-minute restart becomes a forty-minute outage."],
consider:["Populate it in advance from a list of recently requested keys, or simply preload the most popular products.","If the system cannot run without this cache, it needs the same care as a database: failover, capacity planning, and someone who owns it. Calling it just a cache is how that care quietly never happens."]}),
["t","Every one of these is invisible on a normal day and total on a bad one. Naming the stampede and the cold start before being asked is worth more than any Redis vocabulary."]
]}
]};
