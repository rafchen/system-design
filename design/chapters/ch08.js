const D = (name, o) => ["dec", { name, ...o }];
export default {id:"transactions",num:"08",part:"III · Storing data",title:"Transactions and correctness",example:"an e-commerce order system",
tagline:"Making one operation airtight while leaving the rest fast — and what the database is doing underneath when it writes to disk.",
terms:["ACID","Isolation level","Write skew","Conditional write","Deadlock","B-tree","LSM tree","Compaction"],
objectives:["Explain why a transaction alone does not prevent selling the last unit twice","Name write skew and the three ways to prevent it","Choose an isolation level per operation rather than per system","Choose a storage engine from whether the workload reads or appends"],
sections:[
{id:"transactions",title:"Transactions, isolation and the one airtight operation",blocks:[
["p","One unit left in stock. Two customers click Buy in the same millisecond. What stops you selling it twice?"],
["p","Most people answer with a transaction. A transaction on its own does not stop it, and this is worth going through slowly, because it is the most commonly misunderstood idea in the chapter."],
["p","Here is the naive version, wrapped in a perfectly correct transaction: read the quantity; check that it is at least one; subtract one; write it back."],
["p","Both transactions read quantity as 1. Both observe that 1 is at least 1. Both write 0. Two units sold, one existed — and neither transaction misbehaved. Each was atomic. Each was durable. Each was internally consistent from its own point of view."],
["p","This has a name: write skew. Two transactions each read something, each make a decision based on what they read, and each write something that invalidates the other's decision. It survives snapshot isolation, which is the default in a great many databases, because snapshot isolation promises you a consistent view of the data. It does not promise that the view is still true by the time you act on it."],
["p","There are three real fixes. Collapse the check and the write into one atomic statement — UPDATE inventory SET quantity = quantity - 1 WHERE product_id = ? AND quantity >= 1 — and treat zero rows affected as sold out. Or take an explicit lock with SELECT FOR UPDATE and hold it for the length of the transaction. Or run at serializable isolation and retry the transaction when the database aborts it."],
["p","The first is usually right, and it is worth seeing why. The condition and the mutation are evaluated together, by the database, in a single step. There is no gap between deciding and doing for another transaction to slip into, and no lock is held across a network round trip while your application thinks about what to do next."],
["p","Notice that this is the same move as the unique constraint in Chapter 05 and the idempotency key in Chapter 09. Three chapters, three different problems, one shape underneath: never check-then-act in application code. Make the database do both at once, and be willing to be told no."],
["p","One more thing, because it decides where you spend your effort: you do not need this everywhere. Nearly every operation in an order system is perfectly safe at read committed. There are usually one or two that are not — the ones touching money or inventory — and those are the ones you make airtight. Serializable everywhere is not caution. It is a throughput problem you chose to have."],
D("Transaction — several changes that succeed or fail together",{say:"Placing an order writes the order, its lines, and the stock decrement as one unit. Either all of it happened or none of it did, and the database records it durably before telling the customer it worked.",
use:["Several changes in one database that must not be allowed to half-happen."],
avoid:["Spanning services or separate databases. That is Chapter 16.","Wrapping slow work inside a transaction, because it holds locks for the whole duration and blocks everyone else."],
consider:["The guarantees are: all-or-nothing, the database's own rules stay true, concurrent transactions do not corrupt each other, and a confirmed write survives a crash because it was written to a log before being acknowledged.","Readers see a consistent snapshot without blocking writers. Only two writers touching the same row actually conflict.","Two transactions can deadlock by each holding what the other needs. The database detects this and aborts one, and your code has to retry it."]}),
D("Make one operation airtight, leave the rest fast",{say:"Everything runs at the default isolation level except the stock decrement, which is a single statement that checks and subtracts at once. It holds no lock while the application thinks, and it is the one place where a wrong answer costs money.",
use:["A single combined check-and-write, or an explicit lock, or the strictest isolation level — for the one or two operations protecting something that must never be violated: stock, seats, balances.","The ordinary default for everything else."],
avoid:["The strictest isolation everywhere. Under contention it aborts transactions constantly, and you spend your throughput on a guarantee almost nothing needed.","The weakest setting, where you can read changes that were never committed."],
consider:["At the common default, two reads inside one transaction can return different values, because other transactions commit in between.","At snapshot isolation you get a stable view, which is not the same as a guarantee that the view is still true when you act on it.","At the strictest level the database behaves as if transactions ran one at a time, and aborts any that would have conflicted."],
fails:"Two customers each read stock as one. Each concludes one is enough. Each writes zero. Two units sold and one existed — and neither transaction did anything wrong. This is called write skew, and a plain transaction does not prevent it.",
cases:["The last unit is bought twice, so combine the check and the write into one statement.","Two seats and one payment, so cover both rows in one transaction or hold a reservation that expires.","Checking a balance and then debiting it, so either do both in one transaction or make the debit itself conditional on sufficient funds."]}),
["ex","-- the airtight version\nUPDATE inventory SET quantity = quantity - 1\nWHERE product_id = ? AND quantity >= 1;\n-- 0 rows affected → sold out; no lock held across the request"],
["t","Stronger isolation = fewer anomalies + more aborted transactions. Make the one critical operation airtight with a conditional write and leave the rest fast."]
]},
{id:"engines",title:"Storage engine: B-tree or LSM",blocks:[
["p","Two databases can offer identical SQL and behave completely differently under load, because underneath they are writing to disk in opposite ways."],
["p","A B-tree updates in place. To change a row it locates the page holding it and rewrites that page. Reads are predictable — a handful of page reads reaches any key, at any table size — and writes are scattered randomly across the disk."],
["p","An LSM tree never updates anything. Writes land in an in-memory table which is periodically flushed to disk as a sorted, immutable file. Changing a row appends a new entry. Deleting one appends a tombstone. Nothing already on disk is ever modified."],
["p","That sounds wasteful, and it is the opposite, for one physical reason. Sequential writes are enormously faster than random ones. A spinning disk manages a few hundred random operations a second and a few hundred megabytes a second sequentially — three orders of magnitude apart. SSDs narrow that gap without closing it, and random writes wear them out faster."],
["p","The bill arrives on reads and in the background. A read may have to check several files, which is why LSM engines keep a Bloom filter on each one: a small structure that can cheaply say this key is definitely not in here, letting the read skip the file entirely."],
["p","And those files have to be merged, continuously, forever. That is compaction, and it consumes disk throughput that your incoming writes also want. When compaction falls behind, reads get slower, and eventually writes stall waiting for it. This is the LSM tax, and it is what operators actually complain about at three in the morning."],
["p","So: orders are read-heavy, latency-sensitive and updated in place, which is a B-tree. A stream of order events arriving at a hundred thousand a second and never modified afterwards is an LSM. Same system, different verbs, different engine."],
D("B-tree storage — update records where they sit",{say:"Reads here are latency-sensitive and records are updated in place, so a storage engine that rewrites the page holding the row gives predictable lookups. This is what Postgres and MySQL use by default.",
use:["Read-heavy work where lookup time must be consistent, and records change in place. That covers most relational workloads."],
avoid:["Enormous streams of appended data, such as event logs or metrics."],
consider:["Writes land in scattered places across the disk, because the page needing the change could be anywhere.","Reads are consistently a small number of disk reads regardless of table size, which is why latency is stable."]}),
D("Log-structured storage — never modify, always append",{say:"Order events arrive at a hundred thousand a second and are never changed afterwards. An append-only engine turns every disk write into a sequential one, which is dramatically faster than writing in scattered places.",
use:["Write-heavy, append-mostly data: events, messages, metrics. Cassandra and RocksDB work this way."],
avoid:["Point lookups that must be fast on data that is overwritten frequently."],
consider:["Writes go to memory first, then get written out as a sorted file. Changing a record appends a new version; deleting appends a marker. Nothing on disk is ever edited.","A read may have to check several files, so each file carries a small filter that can cheaply say this key is definitely not in here.","Those files must be merged continuously, forever. That merging competes with your writes for disk throughput, and if it falls behind, reads slow down and eventually writes stall. This is the recurring operational complaint about these engines."],
nuance:"The whole thing rests on one physical fact: writing sequentially is enormously faster than writing in scattered places — hundreds of megabytes a second against a few hundred operations a second on a spinning disk. Solid-state drives narrow that gap without closing it. This is also why databases keep a write-ahead log, and why bulk loading is faster than the same rows inserted one at a time."})
]}
]};
