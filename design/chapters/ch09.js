const D = (name, o) => ["dec", { name, ...o }];
export default {id:"apis",num:"09",part:"IV · Exposing it",title:"API design",example:"a payments API",
tagline:"The contract between clients and services, and where one service ends — every choice as: what to say, when, when not, what it costs, how it fails.",
terms:["Cursor vs offset","Idempotency key","Versioning","Rate limiting","Monolith vs microservices","API gateway","Stateless","Control vs data plane"],
objectives:["Pick a pagination style from who walks the list","Make a mutation safe to retry and say where the key lives and for how long","Version and rate-limit without breaking clients","Argue monolith vs microservices from a nameable boundary benefit","Choose sync vs async per call and separate control plane from data plane"],
sections:[
{id:"protocol",title:"Choosing the API protocol: REST, GraphQL or gRPC",blocks:[
["p","A URL shortener is about as small as a useful system gets. Someone hands you a long URL and gets back a short code. Someone else follows that code and lands on the original page. Two operations. That is the whole product, and it is small enough that every decision in this chapter is visible in it."],
["p","Start with the one that sounds like it wants a fashionable answer: how should the client talk to your server?"],
["p","It does not want a fashionable answer. Ask two duller questions instead. Who is the client — a browser, a phone, or another service you control and deploy? And does the client need to choose the shape of the response, or is one shape right for everybody?"],
["p","For the shortener both answers are easy. The client is a browser or a script, so it is not something you control or can upgrade. And there is exactly one shape a redirect can take. Two answers, and the protocol falls out: REST."],
["p","Now change the system and watch the answers change with it. A profile screen in a mobile app needs the user, their last ten posts, the comment count on each, and the avatar of whoever commented most recently. Over REST that is four or five round trips, and on a phone the round trips — not the bytes — are what the user feels. Here the client genuinely does need to choose the shape of the response, and the answer moves to GraphQL."],
["p","Change it once more. Your API server needs a fraud score before it will issue a code for a submitted URL. Both ends of that call are yours. Nobody outside the company will ever make it. It happens on every single write. You are now paying to serialise JSON and parse it back thousands of times a second in exchange for nothing, and the answer moves to gRPC."],
["p","That is the whole method, and it is worth saying plainly because most candidates never do: REST is the default you move away from, not the one you move toward. If you cannot name the client need that made you leave, you have not made a decision. You have expressed a preference."],
D("REST — resources named by URL, actions by HTTP verb",{say:"I would use REST here because the API is about things — links, users, orders — rather than actions, it is used by clients I do not control, and every proxy, browser and cache already understands it without being told anything.",
use:["Public APIs, or any API used by people outside your team.","Creating, reading, updating and deleting records that have a clear identity.","Web and mobile clients.","Responses that benefit from being cached by their URL, since the URL is the whole identity of the thing.","Simple request-and-response traffic, like the shortener's create and redirect."],
avoid:["Screens that need many related pieces of data at once and end up making six to ten separate requests to build one page.","Clients that each want a different subset of the same data.","High-volume calls between your own internal services, where the text format and the extra round trips cost more than they are worth."],
consider:["You will send fields nobody wanted, or force clients to make several calls to assemble one screen. Both are fixable — let clients ask for specific fields, or ask for related records to be included.","Put the version in the path, like /v1/, and only make additive changes within a version.","Status codes carry meaning and clients act on them: 201 means created, 409 means the request was fine but the world is not in the right state, 429 means slow down, 503 means try again later."],
nuance:"REST is not incapable of letting clients choose fields or include related records. GraphQL simply makes that the central idea rather than an add-on.",
alt:"GraphQL when clients need different shapes of the same data. gRPC for internal calls between services you own."}),
D("GraphQL — the client asks for exactly the fields it wants",{say:"I would consider GraphQL because the app needs different combinations of related data on different screens, and over a plain REST API that becomes several round trips per screen, which is what the user actually feels on a phone.",
use:["Several kinds of client — web, phone, television — each needing a different shape of the same data.","Deeply connected data, like a profile with posts, each post with comments, each comment with its author.","A front end that changes faster than the back end can add endpoints for it.","Cases where the number of round trips, rather than the amount of data, is the bottleneck."],
avoid:["Simple create-read-update-delete APIs, where it adds a whole query layer in exchange for nothing.","Public APIs, because you cannot control how expensive a stranger's query will be.","Responses you wanted the content delivery network to cache by URL."],
consider:["A single query can quietly trigger one database read per item returned — ask for fifty posts and get fifty separate author lookups. The fix is to batch those lookups, and you have to build it deliberately.","Someone can write a query nested twenty levels deep. Set limits on depth and on estimated cost, and reject queries that exceed them.","Permissions have to be checked per field rather than per endpoint, because one query can reach data that used to live behind several endpoints.","Caching is harder. Queries are sent as POST bodies, so there is no URL to cache by. You end up relying on saved named queries and client-side caches instead.","Rate limiting by number of requests stops meaning anything, because one request can be enormous. You have to limit by estimated cost."],
alt:"REST with parameters that let the client pick fields and include related records. Or a small dedicated API per client, tailored to that client's screens."}),
D("gRPC — typed, binary calls between services you own",{say:"I would use gRPC between our own internal services, because we control both ends, we want the contract checked at compile time rather than discovered in production, and at this call volume the cost of encoding and parsing text actually shows up in the profile.",
use:["Calls between services inside your own infrastructure.","Teams working in several languages, since the client code is generated from one shared definition.","Streaming, where either side may send a sequence of messages rather than one.","High call volumes, where the time spent turning objects into text and back is measurable."],
avoid:["Browsers. It can be made to work through a translating proxy, but it is never the direct path.","Public APIs, where being able to debug a call with ordinary command-line tools matters more than efficiency."],
consider:["The message format is defined in a schema file and sent as compact binary rather than text, which is where the efficiency comes from and why you cannot read it off the wire.","Set a deadline on every call and pass it down to whatever that call triggers. Without this, one slow service quietly holds up everything upstream of it.","Fields are identified by number, not name. Renumbering a field breaks every client that has not been rebuilt. Adding new fields is safe; reusing an old number is not."],
nuance:"The common arrangement is a browser talking REST or GraphQL to one gateway, and that gateway talking gRPC to everything behind it.",
cases:["The shortener uses REST everywhere. Two endpoints, public, cacheable, and no client needs a different shape.","A payments platform uses REST for its customers and gRPC between payments, fraud and ledger internally."]}),
["h","Why a network call is not a function call"],
["p","gRPC is built to make a remote call look like a local one. You call a method, you get a return value, and the network is hidden. That is the point of it, and it is also the one thing about it worth being nervous about."],
["p","A local function call has two outcomes: it returns, or your process has a much larger problem. A network call has four."],
["p","It succeeds and you hear so. It fails and you hear so. It succeeds and the response is lost on the way back — the work happened, and you have no idea. Or it is still running when you stop waiting, and it may finish correctly one second after you have already decided it did not."],
["p","The third and fourth have no equivalent inside a single process, and no amount of syntactic sugar removes them. A framework that makes the network invisible has not made it reliable; it has only made it easy to forget."],
["p","Chapter 18 is built entirely on those four outcomes. Notice them here, at the exact point where the abstraction first invites you to forget them."],
["t","REST is the safe default; you justify moving off it, never onto it. GraphQL trades server simplicity for client flexibility; gRPC trades debuggability for efficiency and contracts."]
]},
{id:"contract",title:"Resource modelling, status codes and validation",blocks:[
["p","A payments API is a promise made to somebody else's code. Unlike a function signature, which you can change on a Tuesday afternoon, you cannot take it back. Every decision in this chapter is shaped by that."],
["p","Here is a real one with a real answer. Should refunding a payment be POST /v1/refundPayment, or POST /v1/payments/{id}/refunds?"],
["p","The second, and not because it is tidier. The second says a refund is a thing. It has an id. It has a status. It can be listed, and it can itself fail and be retried. The first says a refund is something that happens to a payment and leaves nothing behind."],
["p","The moment support asks which refunds are currently pending, the first design has no answer and the second has a GET. Modelling nouns is not a style preference — it is a bet that anything worth doing is worth being able to look at afterwards."],
["p","Status codes are part of the same contract, and the gap between 409 and 422 is not pedantry. A 409 means your request was fine but the world is not in the right state: you cannot refund more than was charged, not yet anyway. A 422 means the request itself was wrong and will be wrong forever."],
["p","The first is worth retrying later. The second never is. A client that cannot tell them apart will either retry forever or give up immediately, and both of those are your fault, not theirs."],
D("Model the domain as things, not actions",{say:"I model the domain as nouns — payments, refunds, customers — and let the HTTP verb carry the action. That makes the API predictable, and every cache and proxy already understands what a GET means.",
use:["Any REST API."],
avoid:["Putting verbs in the URL, like /createPayment. It is a sign the underlying model has not been worked out, and it leaves the result of the action with no identity of its own."],
consider:["The shape is POST /v1/payments to create one, GET /v1/payments/{id} to read it, POST /v1/payments/{id}/refunds to refund it. A refund is a thing with its own id and status, so you can list refunds and retry a failed one.","Status codes have to mean specific things, because clients act on them. 201 created. 404 unknown. 409 the request was fine but the world is not in the right state. 422 the request itself was wrong. 429 slow down. 503 try later.","Validate at the boundary. An amount of minus five hundred, or a currency of dollars rather than usd, should never reach the code that moves money."],
cases:["A refund larger than the original charge should be a 409, not a server error.","An unrecognised field arrives in the request. Decide whether you ignore it or reject it, and write that decision down."],
nuance:"Be specific about which field failed validation, and deliberately vague about authentication failures. Never say the account does not exist — say the credentials are invalid."})
]},
{id:"pagination",title:"Offset or cursor pagination",blocks:[
["p","GET /v1/payments returns a list. How much of it?"],
["p","This sounds like the least interesting decision in the chapter. It is the one most likely to be wrong in a way nobody notices for a year."],
["p","Take the obvious approach. Page one is the twenty newest payments. Page two is OFFSET 20. A user opens page one and reads it. While they read, three payments arrive. They click through to page two — and the three payments they already saw on page one have been pushed down into it. They see three duplicates, and three other payments they will never see at all."],
["p","That is not an edge case. That is what happens to any list that grows while somebody is reading it, which is every list worth paginating."],
["p","A cursor fixes it by remembering where you were rather than how far in you were. Encode the sort key of the last item returned, and the next page asks for everything after that point. New rows arriving above your position no longer shift you, because your position was never a count."],
["p","It is also dramatically cheaper. OFFSET 100000 does not skip a hundred thousand rows — it walks them and throws them away, every time, so the last page of a long list is the slowest query in your system."],
["p","What you give up is jumping to page forty. That is a genuine loss for a human clicking through an admin table and no loss whatsoever for a script walking an export. Humans and small tables, offset. Machines and unbounded tables, cursor."],
["p","One trap worth knowing. The sort key has to be unique, or the cursor will silently skip or repeat rows at the boundary. A timestamp alone is not unique — two payments in the same millisecond are entirely ordinary — which is why the cursor carries the id alongside it as a tiebreaker."],
D("Cursor pagination — remember where you were, not how far in",{say:"Machines walk this list and it grows without limit, so I return an opaque marker representing the last item sent. The next page continues from that point. It costs the same no matter how deep you go, and new rows arriving do not shift what you see.",
use:["Feeds, logs, exports, webhooks — anything a client reads all the way through.","Any table that grows without bound."],
avoid:["Admin screens where a person wants to click straight to page forty."],
consider:["The value you sort by has to be unique, or the marker will skip or repeat rows at the boundary. A timestamp alone is not unique, because two records in the same millisecond are ordinary, so include the id as a tiebreaker.","There is no way to jump to an arbitrary page, because the client only knows where it stopped.","Keep the marker opaque — encode it — so clients cannot build their own and depend on its internals."],
fails:"Sorting on a column that is not unique silently skips or duplicates rows at page boundaries. And a marker that exposes raw ids lets somebody work out how many records you have and guess at others."}),
D("Offset pagination — skip a count, take the next twenty",{say:"This is a small table that a person browses, so asking for page three is fine and lets them move around freely.",
use:["Small tables browsed by people.","Cases where jumping to a specific page matters more than perfect consistency."],
avoid:["Large tables. Skipping a hundred thousand rows does not skip them — the database walks and discards every one, on every request.","Lists that change while being read. New rows arriving push everything down, so the reader sees some records twice and never sees others at all."]}),
D("Only offer filters and sorts you have an index for",{say:"I expose only the filters and sort orders that an index can serve, and I treat adding a new filter parameter as a schema change rather than a small feature.",
use:["Filters that map onto an index you already have."],
avoid:["Letting clients sort by any column they like. That is a full table scan wearing a query parameter."],
consider:["The order of columns in a multi-column index has to match how the query filters and sorts, which Chapter 07 goes into."]})
]},
{id:"idempotency",title:"Idempotency keys",blocks:[
["p","This is the most important section in the chapter, and it exists because of the four outcomes from Chapter 04."],
["p","A client sends POST /v1/payments. The card is charged. The response is lost on the way back — a load balancer recycles, a phone switches from wifi to cellular, anything at all. The client sees a timeout. What should it do?"],
["p","It has two options and both are wrong. Retry, and possibly charge the customer twice. Do not retry, and possibly never charge them while telling them it worked."],
["p","There is no third option available to the client. Which means the third option has to be built by the server."],
["p","An idempotency key is that third option. The client generates a unique value, sends it with the request, and promises to send the same value on every retry of that same intent. The server records the key with the response. When the key arrives a second time, the server does not do the work again — it replays what it said the first time."],
["p","Now retrying is safe. Because retrying is safe, the client can always retry. Because the client can always retry, the lost response stops being a dilemma and becomes routine."],
["p","One detail decides whether any of this works. Claiming the key and doing the work must not be separable in the wrong order. If you charge the card and then write the key, a crash in the gap leaves a charge with no record of it, and the retry charges again — which is precisely the bug the mechanism was built to prevent."],
["p","So claim the key first, with a unique constraint, before anything irreversible happens. That is the same move as the shortener's colliding code in Chapter 05: do not coordinate, let the database refuse."],
["p","Keys need an expiry, and it is worth naming one. Too short and a genuinely slow retry is treated as a new request. Twenty-four hours is the usual answer, and saying it shows you have thought about the fact that this table grows forever otherwise."],
D("Idempotency key — make a retry safe to send",{say:"A lost response makes the client retry, so every request that creates or charges carries a unique value the client generates. We store the response against that value and replay it if the same value arrives again, which means a retry can never charge twice.",
use:["Payments, orders, transfers, sending messages — any change a client might reasonably resend.","Requests that carry many items, where each item gets its own key."],
avoid:["Reads and deletes, and updates that set a value outright. Those are already safe to repeat.","Low-stakes events like analytics, where an occasional duplicate costs nothing."],
consider:["Claiming the key and doing the work must not be separable in the wrong order. If you charge the card and then record the key, a crash in between leaves a charge nobody knows about, and the retry charges again — the exact bug this was meant to prevent. Claim it first, using a uniqueness rule so two simultaneous attempts cannot both succeed.","If the first attempt is still running when the retry arrives, return 409 rather than starting a second one.","Scope keys per customer so two customers cannot collide by chance.","Store the entire response, not just a note that it succeeded, so the retry receives identical output.","Keys need an expiry. Twenty-four hours is usual. Too short and a genuinely slow retry is treated as new; too long and the table grows forever."],
fails:"The key store becomes unavailable. Now you choose: refuse all payments, or process them and risk duplicates. Decide which in advance and say so.",
alt:"Have the client generate the resource id itself and enforce uniqueness on it — the same idea with one table fewer. For work triggered by messages rather than requests, Chapter 17 covers the equivalent.",
cases:["A user double-clicks Pay, so the same key arrives twice and gets the same response.","A client retries with the same key but a different body, which should be rejected as a conflict.","Two browser tabs generate two different keys for the same intent, producing two charges. That is the client's bug, but document it so they know."]}),
["ex","POST /v1/payments\nIdempotency-Key: 8f1c2e0a-…\n{ \"amount\": 4200, \"currency\": \"usd\", \"customer\": \"c_42\" }"],
["t","One extra table and one TTL to choose, versus double charges under exactly the failure that is hardest to reproduce. For money it's mandatory; for a like button it's optional."]
]},
{id:"versioning-auth",title:"Versioning, auth placement and rate limiting",blocks:[
["p","You are going to change this API. The only question is whether the change breaks somebody."],
["p","Adding a field is safe if — and only if — clients ignore fields they do not recognise. That is a rule you have to publish, loudly, because a client that validates strictly will turn your harmless new optional field into an outage on their side and a support ticket on yours."],
["p","What you are asking for has a name: forwards compatibility, meaning old code has to tolerate data written by new code. It is the harder direction of the two, because the old code was written before anyone knew what was coming, and the only way to get it is to have told people the rule in advance."],
["p","Everything that cannot be done additively is a v2, with a deprecation window and usage metrics on v1 — because you cannot switch off a version you are not measuring, and you will not be brave enough to try."],
["h","Who you are, and what you may touch"],
["p","Two words that get used interchangeably and must not be."],
["p","Authentication is who you are. Authorization is what you may do, and crucially, to which object."],
["p","The gateway can settle the first once, for everybody. It cannot settle the second, because it does not know that payment pay_8813 belongs to account acct_42. Only the payments service knows that."],
["p","So if the gateway validates the token and the service then trusts it blindly, a completely valid token can read every payment in the system by changing one id in a URL. This is the single most common finding in real API security audits. It is not an exotic attack. It is changing a number."],
D("Versioning — change the API without breaking anyone",{say:"Anything additive ships inside the current version. Anything that breaks existing clients becomes a new version, with a published end date for the old one and usage figures so we know who is still on it.",
use:["Any API with clients outside your own team."],
avoid:["Changing what an existing field means without changing the version. Clients cannot detect that, so it fails silently in their code.","Versioning individual fields. Keep versions coarse."],
consider:["Tell clients in writing to ignore fields they do not recognise. Without that rule, adding an optional field breaks anyone validating strictly — and it will be your fault by the time it reaches support.","Putting the version in the path is more visible; putting it in a header is cleaner. Either works. Pick one and be consistent."]}),
D("Check who you are once; check what you may touch every time",{say:"The gateway validates the token once, for everybody. The service then checks on every request that this specific payment belongs to this specific caller — otherwise a perfectly valid token reads anyone's data by changing a number in the URL.",
use:["Every API, without exception."],
avoid:["Treating a valid token as proof of permission. This is consistently the most common serious finding in real API security audits, and it is not a sophisticated attack — it is editing a URL."],
consider:["Authentication is who you are. Authorization is what you may do, and to which specific object. The gateway can settle the first and cannot possibly settle the second, because it does not know who owns what.","Chapter 20 covers how tokens themselves work."]}),
D("Rate limiting — cap how fast any one caller can ask",{say:"Each API key gets an allowance that refills steadily and permits short bursts. Going over it returns 429 with a header saying when to try again, and the expensive endpoints get a tighter allowance than the cheap ones.",
use:["Every public endpoint.","Protecting yourself from your own customers' bugs, which cause more incidents than deliberate abuse does."],
avoid:["Limiting only by network address. A whole office shares one address, so you would throttle an entire company because of one person.","Applying the limit after the request has already done the expensive work. Reject early, before it costs anything."],
consider:["The counters have to be shared across all your servers, which usually means Redis — and that makes Redis something your API now depends on. Decide what happens when it is unavailable: fall back to a rough per-server limit rather than failing every request.","Counting requests is simple to explain. Charging by how expensive each request is, is more accurate and much harder to explain. Start simple and add weights for the two or three endpoints that actually hurt."]}),
D("Batch endpoints — many items in one request",{say:"Sending fifty payments in one call removes forty-nine round trips. Each item carries its own idempotency key and gets its own result, so a partial failure is reported per item rather than failing the whole batch.",
use:["High-volume clients, and mobile clients syncing after being offline."],
avoid:["Cases where the items must all succeed or all fail together. That is a transaction, and a batch is not one."],
consider:["Some items succeeding while others fail is the normal case, not an error case. Return a status for each one.","Put a limit on batch size, or somebody will send a hundred thousand items in one request."]})
]}
]};
