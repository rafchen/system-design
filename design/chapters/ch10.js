const D = (name, o) => ["dec", { name, ...o }];
export default {id:"distributed",num:"10",title:"Distributed systems theory",example:"a bank transfer between two accounts",
tagline:"Consistency, coordination and cross-machine transactions as decisions — which model per operation, which mechanism, what it costs, how it fails.",
terms:["Linearizable vs eventual","Read-your-writes","CAP / PACELC","Raft","Quorum","Fencing token","2PC vs saga","Outbox / CDC"],
objectives:["Choose the weakest consistency model each operation tolerates","State CAP and PACELC as per-operation choices, not a system label","Choose consensus, quorum or a single leader for coordination and prevent split brain","Choose 2PC or a saga for a concrete cross-service workflow","Guarantee event publication with an outbox and achieve effectively-once end to end"],
sections:[
{id:"consistency",title:"Choosing a consistency model per operation",blocks:[
["p","Alice sends Bob fifty pounds. What must never be true?"],
["p","That the money left Alice and did not arrive at Bob. Or that it arrived at Bob and never left Alice. Every mechanism in this chapter exists to protect a sentence of that shape."],
["p","Notice the shape carefully. It is not the system is consistent. It is a specific claim about a specific pair of facts that must not be allowed to disagree. Consistency is not a setting you turn on. It is a per-operation choice, and different operations in the same product should choose differently."],
["p","In this bank the balance is linearizable: every reader sees the effect of every completed transfer, in order, without exception. The list of past transfers may lag by a few seconds, because nobody is harmed by a statement that is briefly incomplete. The monthly interest projection can be an hour stale and no one will ever know."],
["p","One product, three answers. Being able to say which operation gets which is most of the skill here."],
["p","So the instruction is: name the weakest model each operation can tolerate, then name the mechanism that provides it. Strong consistency is not the safe default. It costs a coordination round trip on every write and it reduces availability during a partition, and paying that for a like count is not caution — it is waste with a respectable-sounding name."],
["tbl",{cols:["Model","Use when","Tradeoff · example"],rows:[
["Linearizable (strong)","Stale or wrong data is unacceptable","Coordination on every write · account balance, seat ownership"],
["Serializable","Transactions must not interleave badly (not about recency)","Aborts under contention · the debit+credit transaction"],
["Causal","Related operations must appear in order","Extra metadata · reply never shown before its parent"],
["Read-your-writes","Users must see their own change now","Routing or version tracking · profile edit, 'my transfers' list"],
["Monotonic reads","Never see time go backwards","Session pinning · a feed that shouldn't lose items on refresh"],
["Eventual","Temporary staleness is fine","Stale reads · like counts, analytics, search index"]]}],
D("Strong consistency — every reader sees every completed write",{say:"The debit and the credit must be seen together. A reader that sees the money leave Alice but not arrive at Bob is exactly how money appears to vanish. The list of past transfers, on the other hand, can lag by seconds without harming anyone.",
use:["Money, stock, seats, uniqueness such as usernames, and permissions."],
avoid:["Counters, feeds, search results and analytics. You pay in speed and availability for something no user will ever notice."],
consider:["It costs a round trip to another machine, or to a group of machines, on every write. That cost is permanent and paid on every request.","Saying the whole system is strongly consistent is as much a warning sign as saying it is all eventually consistent. Real systems choose per operation."],
cases:["A transfer appears on Alice's statement before Bob's balance updates, which is fine as long as the two are linked so nobody sees them out of order.","Two transfers race on one account, so the debit itself must check the balance rather than the application checking first."]}),
["h","CAP, stated so that it means something"],
["p","You will be asked about CAP. The version most people recite — pick two of the three — is wrong in a way that makes it useless."],
["p","You do not get to pick whether partitions happen. A cable is cut, a switch reboots, a rack loses connectivity. Partitions are events, not options, and no amount of architecture opts out of them."],
["p","So the theorem is not offering three things to choose two from. It is telling you that when a partition occurs, you must pick one of two behaviours. Answer anyway, with data that may be stale or may later conflict. Or refuse to answer until you can be certain. Those are the only two, because the third — remain correct and available while cut off from the other half — is exactly what the theorem says cannot exist."],
["p","For the balance, refuse. An error is recoverable; a wrong balance that somebody acts on is not. For the transfer list, answer with what you have and mark it as possibly delayed."],
["p","Then there is the part CAP says nothing about, which matters far more day to day. There is no partition happening right now. What are you doing the other 99.9% of the time?"],
["p","That is what PACELC adds: else, choose between latency and consistency. Waiting for a synchronous replica on every write costs a few milliseconds forever, partition or no partition. That is the cost your users actually feel, every single day, and the famous theorem is silent about it."],
D("What to do when the network splits — and the rest of the time",{say:"For the balance I choose correctness: if we cannot reach the other half, we refuse the write. For the transfer list I choose availability: serve what we have and mark it as possibly delayed. And in normal operation I accept a few milliseconds per write to keep a copy in step.",
use:["State this per operation, exactly as above."],
avoid:["Reciting pick two of three. You do not get to choose whether the network splits — it will — so it was never a menu of three.","Labelling a whole system as one or the other. Different operations in one product should choose differently."],
consider:["When a split happens you have exactly two options: answer anyway with data that may be wrong, or refuse to answer until you can be sure. There is no third option, and that is the entire content of the theorem.","Refusing to answer is often the right choice. An error can be retried; a wrong balance somebody acts on cannot be undone.","Most of the time there is no split at all. Then the real question is whether you wait for another machine on every write, trading speed for certainty. That is the cost users actually feel daily, and the famous theorem says nothing about it."],
nuance:"For money, an error is recoverable and wrong data is not. For a social feed it is the other way round. That single sentence decides most of these choices."})
]},
{id:"coordination",title:"Coordination: consensus, quorums, leases",blocks:[
["p","Two workers wake up and both conclude they are responsible for processing transfer T. How do you ensure only one of them proceeds?"],
["p","The obvious answer is a lock. The obvious answer contains a subtle and famous bug, and it is worth walking through in full because it changes how you think about every distributed mechanism after it."],
["p","Worker one takes a lock with a thirty-second expiry and begins work. Then its process pauses. Not crashes — pauses. A garbage collection, a hypervisor scheduling it out, a burst of page faults. It is simply not running for forty seconds."],
["p","The lock expires. Worker two acquires it entirely legitimately and processes the transfer. Then worker one resumes. From its own point of view, no time has passed. It believes it still holds the lock, because the last thing it knew was that it did, and it completes its work. The transfer happens twice."],
["p","Nothing here malfunctioned. The lock service did exactly what it promised. The pause is ordinary — every managed runtime does it, and no runtime warns you first. The bug is the assumption that holding a lock a moment ago means holding it now, and a paused process has no way whatsoever to tell the difference."],
["p","The fix is a fencing token. The lock hands out a number that only ever increases: worker one gets 33, worker two gets 34. Every write to the protected resource carries its token, and the resource refuses any token lower than the highest it has already accepted. When worker one wakes and writes with token 33, the account store rejects it — not because it noticed the pause, but because it simply does not take old tokens."],
["p","That is the move worth extracting. Correctness does not depend on anyone detecting the failure. It depends on the protected resource being able to refuse. A lock that nothing checks is a lock that lies."],
["h","And do not trust the clock either"],
["p","The same distrust applies to time. It is tempting to order two transfers by their timestamps. Do not. Clocks on different machines disagree, and worse, NTP can step a clock backwards, so a later event can carry an earlier time and there is no way to detect it after the fact."],
["p","Ordering comes from a sequence the system controls: a per-account sequence number, a logical clock, a consensus log. Never a wall clock. Last-writer-wins by timestamp is a data-loss strategy with a friendly name."],
["p","Which leaves consensus — Raft, Paxos — as the way a group of machines agrees on one value when any of them may fail. It is correct and it is slow, because every decision costs a round trip to a majority. So spend it on the small amount of state that must never disagree, like who the leader is and what the configuration says, and keep your application data well away from it."],
D("Consensus — getting a group of machines to agree on one thing",{say:"Leader election and configuration go through a group of three machines that require a majority to agree. Only one side of a network split can hold a majority, so only one side can elect a leader, and each leadership term carries a number that lets everyone reject an outdated one.",
use:["Deciding which machine is in charge, storing configuration, and any small piece of state that absolutely must not be disagreed about."],
avoid:["Your application data. These groups are three to five machines and every decision costs a round trip to a majority. That is the wrong shape for volume."],
consider:["A group of 2F+1 machines tolerates F failures. Three machines survive one failure; five survive two.","The leader sends regular signals; if they stop, the others wait a randomly chosen interval before proposing themselves, which stops everyone proposing at once.","Any newly elected leader is guaranteed to already have every decision that was previously agreed.","This is what etcd, ZooKeeper and Consul do, and what sits underneath most databases that split data across machines."],
fails:"The side without a majority stops accepting writes, which is correct but looks like an outage. A slow disk on the leader slows every decision. And a group of two machines has no majority after any failure, which makes it worse than a group of one."}),
D("Quorums — overlap reads and writes so they cannot miss each other",{say:"With three copies, writing to two and reading from two guarantees that any read touches at least one machine that saw the latest write. That gives correctness without needing a leader at all.",
use:["Systems with no leader, where you can tune the numbers per operation."],
consider:["The rule is that copies written plus copies read must exceed the total. Two plus two exceeds three, so they overlap.","Lower the write count for faster writes, or the read count for faster reads, and you lose the overlap guarantee in exchange.","When a machine is unreachable, some systems will write to a substitute and hand the data over later. That keeps you available and quietly breaks the overlap guarantee while it is happening."]}),
D("Leases and fencing tokens — a lock that cannot lie",{say:"Two workers must not process the same transfer, so a worker takes a time-limited claim that comes with a number that only ever increases. Every write carries that number, and the account store rejects anything with an older one — so a worker that froze and woke up late is refused.",
use:["Making sure only one worker processes a given job or record.","Electing one machine to perform a duty within a pool."],
avoid:["Trusting a lock without the protected resource checking anything. That is a lock that lies, and it will."],
consider:["A claim has to expire, because the holder might have died. But expiry is exactly what allows two holders to overlap, so expiry alone is never enough.","The number must strictly increase, and the resource being protected must refuse anything lower than the highest it has already accepted."],
fails:"Worker one takes a thirty-second claim, then its process freezes for forty seconds — a garbage collection, or the machine being descheduled. The claim expires. Worker two takes it legitimately and does the work. Worker one wakes up believing no time has passed and does the work again. Nothing malfunctioned, and the transfer happened twice.",
cases:["A scheduler picking jobs that are due, covered in Chapter 14.","Exactly one consumer per partition of a message log.","A failover that must promote exactly one new leader."]}),
D("Do not order events by the clock",{say:"I would not order writes by timestamps. Clocks on different machines disagree, and time synchronisation can move a clock backwards, so a later event can carry an earlier time. Ordering comes from a counter the system controls.",
use:["A per-record sequence number, or a logical counter that only increases, whenever order matters."],
avoid:["Resolving conflicts by keeping whichever write has the later timestamp. That is a data-loss strategy with a reassuring name."],
consider:["Time synchronisation can step a clock backwards, so an event really can appear to happen before one that preceded it.","Google's Spanner solves this properly using atomic clocks and satellite receivers in every data centre. You do not have those."]}),
["t","Consensus is correct and slow; use it for the small amount of state that must never disagree (who leads, config), and shard application data into many independent groups."]
]},
{id:"cross-service",title:"Transactions across services: 2PC or saga",blocks:[
["p","The transfer is two writes in two places: debit Alice on one shard, credit Bob on another. There is no transaction that spans them."],
["p","The textbook answer is two-phase commit. A coordinator asks every participant to prepare — do the work, hold the locks, promise you can commit — and then, once all have voted yes, tells them to commit."],
["p","Now the failure that defines the protocol. The coordinator dies after everyone has voted yes and before it says commit. Every participant is holding locks, has promised it can commit, and is not permitted to commit or abandon on its own. They wait. They keep waiting, holding those locks, until the coordinator comes back."],
["p","That is not a rare edge case; it is the protocol working as designed, and it is why 2PC lives inside a database coordinating its own shards and almost never between services you deploy separately."],
["p","The alternative is to stop trying to be atomic and to be honest instead."],
["p","A saga performs the work as a sequence of local transactions, each committing on its own. Create the transfer as PENDING. Debit Alice. Credit Bob. Mark COMPLETED. If crediting Bob fails, you do not roll back — you cannot, the debit already committed — you compensate, by crediting Alice again, and mark the transfer FAILED."],
["p","The price is a state a user can observe. For a moment, the money has left Alice and not reached Bob. You cannot hide that, so you model it: PENDING is a real column value and a real word on the screen. Every real bank shows it to you, and now you know why."],
["p","The second price is that compensation is business logic rather than a database feature, and it can fail too. Which is why a saga always ships with a reconciliation job — something that periodically hunts for transfers stuck in PENDING and drives them to an end state."],
["p","That is worth sitting with, because it is the general lesson of the whole chapter. You do not eliminate the bad intermediate states. You make them visible, bounded, and recoverable."],
D("Two-phase commit — everyone promises, then everyone commits",{say:"Inside one database coordinating its own machines, for a short transaction, this is fine. Across separate services or regions I would not use it, because while the coordinator is down every participant is stuck holding locks.",
use:["Inside a distributed database, for short transactions.","Cases where every participant is in the same data centre and under the same operational control."],
avoid:["Across services you deploy separately, across regions, or for anything long-running."],
consider:["First everyone is asked to prepare: do the work, hold the locks, and promise you can finish. Then, once all have agreed, everyone is told to commit.","If the coordinator dies between those two steps, participants have promised and cannot proceed or abandon on their own. They wait, holding locks, until it returns. That is the protocol working as designed."]}),
D("Saga — a sequence of steps, each with an undo",{say:"Debit Alice, credit Bob, mark it complete — each a separate committed step, each safe to repeat, all keyed on the transfer id. If crediting Bob fails we cannot roll back, so we credit Alice again and mark the transfer failed. The pending state is real, so we show it.",
use:["Work spanning several services: order, then payment, then shipping.","Anything long-running enough that holding locks throughout is unacceptable."],
avoid:["Steps that cannot be undone — an email sent, a parcel physically dispatched — unless you design the compensation as a real business action such as a recall or an apology.","Cases where an observable in-between state is genuinely unacceptable."],
consider:["One service can drive the whole sequence and record progress, which is easier to debug. Or each service can react to the previous one's event, which removes the central component and makes the flow harder to follow.","Every step must be safe to run twice, keyed on the same identifier.","Pending and failed have to exist in your schema and in your interface. Every real bank shows them to you.","Compensation can itself fail, so you need a job that periodically finds anything stuck and drives it to a conclusion."],
fails:"A compensation fails and needs its own retries and alerting. The coordinator dies mid-sequence, which is why progress must be recorded durably rather than held in memory. Or a step that was not safe to repeat gets retried and happens twice.",
cases:["Payment succeeds but shipping is down, so the order sits in a paid-awaiting-shipment state and is retried rather than refunded.","Crediting Bob fails after Alice was debited, so Alice is credited back and the transfer is marked failed."]}),
["ex","Transfer as a saga\n1. Create transfer T, PENDING           (transfer service, local tx)\n2. Debit A referencing T                (shard 1, local tx)  fail → T FAILED\n3. Credit B referencing T               (shard 2, local tx)  fail → re-credit A; T FAILED\n4. T COMPLETED\nEvery step idempotent on T.id — any step can be retried safely."],
["t","2PC gives atomicity and holds locks across a network round trip. Sagas give availability and expose intermediate states your data model must represent. Money systems mostly run sagas with explicit pending states and reconciliation."]
]},
{id:"outbox",title:"Publishing events reliably: outbox, CDC, effectively-once",blocks:[
["p","One more thing must happen once the transfer completes: other systems need to know. Statements, the fraud model, notifications."],
["p","The obvious implementation is two lines — commit the transaction, then publish the event — and it is broken in both directions."],
["p","Crash between them and the transfer happened while nobody was told. Publish first and let the transaction fail, and you have announced a transfer that does not exist."],
["p","There is no ordering of those two operations that is safe, because they are two different systems and you cannot commit to both at once. This is the dual-write problem, and it has no clever fix."],
["p","What it has is a reframing. Write the event into the same database, in the same transaction as the transfer itself, into a table called the outbox. Now there is one commit, and it is atomic by construction: either the transfer and its event both exist, or neither does."],
["p","A separate relay process then reads unsent rows and publishes them. If it crashes mid-publish it will republish on restart, so consumers may see the same event twice."],
["p","Which brings the last idea in the chapter, and it is worth being firm about. Exactly-once delivery does not exist. A consumer can always crash in the gap between acting and acknowledging, and no broker setting closes that gap."],
["p","What you can have is exactly-once effect: at-least-once delivery, plus a consumer that records which event ids it has already handled in the same transaction as whatever it does about them. Deliver repeatedly, act once."],
["p","At-least-once delivery and an idempotent consumer. That combination is what people mean when they say exactly-once, and saying it precisely instead of loosely is worth a great deal in an interview."],
D("Outbox — write the event in the same transaction as the change",{say:"The transfer row and the event announcing it are written in one transaction, into the same database. A separate process then reads unsent events and publishes them, and consumers discard duplicates by event id.",
use:["Any service that must publish an event after changing its own data."],
avoid:["Writing the row and then publishing separately. A crash between the two either loses the event or announces something that did not happen, and there is no ordering of the two that is safe."],
consider:["The publishing process delivers at least once, so consumers must tolerate seeing the same event twice.","Publish in order per record, so events about one transfer are never reordered.","Delete or archive sent rows, or the table grows without limit."],
alt:"Read the database's own change log directly and turn changes into events.",
fails:"The publisher falls behind and events are delayed by minutes with nothing obviously broken. The table grows unbounded. Or a consumer that is not safe to run twice processes a duplicate and acts twice."}),
D("Change data capture — turn the database's own log into events",{say:"A tool reads the database's internal change log and publishes every committed change as an event. There is no outbox code to write, at the cost of tying consumers to our table structure unless we transform the stream first.",
use:["Keeping search indexes, caches and reporting systems in step with an existing database.","Cases where many tables need publishing and writing outbox code for each is impractical."],
avoid:["Cases where you want a meaningful business event rather than a row-level change. Column added is not the same as payment completed."],
consider:["Changing a column now potentially breaks every consumer, so schema changes become a coordination problem.","A new consumer needs a snapshot of current state before it can start following the stream, or it only ever sees changes from now on."]}),
D("Exactly-once effects — deliver repeatedly, act once",{say:"No message system can deliver exactly once to a consumer that might crash between doing the work and confirming it. What we can have is at-least-once delivery plus a consumer that records the event id in the same transaction as its effect — which produces exactly-once effects.",
use:["Every consumer that changes anything."],
avoid:["Believing exactly-once is a setting you can switch on. The gap between acting and acknowledging cannot be closed by the broker, because the broker is not the one acting."],
consider:["Keep a table of processed event ids per consumer, with an expiry, and write to it in the same transaction as the effect itself.","Better still, make the effect naturally repeatable — writing to the same computed location produces the same result twice — and you need no table at all."]}),
["t","Outbox + relay + idempotent consumer is three moving parts. The alternative silently loses or duplicates events under exactly the failures you can't reproduce. For money, it's not optional."]
]}
]};
