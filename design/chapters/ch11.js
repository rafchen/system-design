const D = (name, o) => ["dec", { name, ...o }];
export default {id:"reliability",num:"11",title:"Reliability and handling failure",example:"a checkout service on Black Friday",
tagline:"Every network call has four outcomes. Each reliability pattern as a decision: what it protects, when to use it, when not, what it costs, how it goes wrong.",
terms:["Four outcomes","Timeout","Backoff + jitter","Retry budget","Circuit breaker","Bulkhead","Load shedding","Degradation","RPO / RTO"],
objectives:["State the four outcomes and what the caller does in each","Write a retry policy with backoff, jitter, budget and idempotency","Place breakers, bulkheads and shedding in a request path and say whom each protects","Explain how a cascade starts and three ways to stop it","Set RPO/RTO per component and choose backup, replication or multi-zone from them"],
sections:[
{id:"four-outcomes",title:"The four outcomes of every call",blocks:[
["p","Checkout calls the payment provider. The call times out. Was the card charged?"],
["p","You do not know. That is not a gap in your monitoring or a flaw in your code. It is a property of networks, and it is the most important sentence in this chapter."],
["ex","1 Succeeds, response arrives      — happy path\n2 Fails, error arrives            — recoverable, you know\n3 Succeeds, response lost         — the card WAS charged; you got a timeout\n4 Still running when you give up  — may succeed a second later, or not"],
["p","The first two are the ones everyone designs for. The third and fourth are where money is lost."],
["p","A response you did not receive is not evidence that the work did not happen. Somewhere between the provider committing the charge and those bytes reaching you, something was dropped — and now the customer's card is debited while your orders table believes nothing occurred."],
["p","Which is why every arrow in the diagram needs an answer prepared in advance. Retry with the same idempotency key from Chapter 06, so that the retry costs nothing if the charge did happen and fixes things if it did not. Then reconcile against the provider's own records on a schedule, because the retry can fail too and something has to notice."],
["p","In an interview, the examiner will walk your diagram and ask, for arrow after arrow, and if that times out? Having a real answer each time is most of what this chapter is worth."],
D("The response you never received",{say:"For every arrow in this diagram: if the response is lost, the caller retries with the same key, so a repeat is free if the work already happened. And a nightly comparison against the provider's own records catches whatever the retry missed.",
use:["Every network call, and especially anything involving money or stock."],
avoid:["Assuming calls either clearly succeed or clearly fail. That is true in development and false in production several hundred times a day."],
consider:["A key attached to the request makes retrying safe, which is what Chapter 06 was building toward.","Comparing your records against the other system's records catches the cases where even the retry failed. Somebody has to notice, and it should not be the customer.","The interviewer will ask what happens if this times out for arrow after arrow. Having a real answer each time is most of what this chapter is worth."],
cases:["The charge succeeded at the provider and we timed out. Retrying with the same key returns the original result rather than charging again.","The order was written but publishing the event was lost, which is what the outbox in Chapter 10 prevents."]})
]},
{id:"timeouts-retries",title:"Timeouts, retries, backoff, jitter, budgets",blocks:[
["p","A dependency hangs. How long do you wait?"],
["p","Thirty seconds is the answer that causes the outage, and one global timeout applied to every call is that same answer wearing a disguise."],
["p","Set it from each dependency's own p99 plus a margin. The fraud check normally answers in forty milliseconds, so a hundred and fifty is generous and anything slower was never going to arrive in time to be useful. Card capture normally takes two seconds, so five. Those are different numbers because they are different dependencies, and one value is wrong for both of them."],
["p","The reason the number matters so much is not the waiting itself. A request blocked on a hung dependency is holding a thread, a connection, and a slot in every pool between it and the user. Waiting thirty seconds does not merely make one customer wait thirty seconds. It withdraws capacity from everybody else for thirty seconds."],
["h","Why retries are dangerous"],
["p","Now the arithmetic that turns a helpful mechanism into a harmful one."],
["p","A dependency starts failing half its calls. Every client retries three times. That dependency's incoming load has just tripled, at the exact moment it is least able to absorb it. The retries are not helping it recover. They are the reason it cannot."],
["p","So retries come with three constraints. Retry only what could plausibly succeed: timeouts and 503s, never a 4xx and never a declined card, because no number of attempts will change a decline. Back off exponentially and add jitter — without jitter, a thousand clients that failed together retry together, in synchronised waves, and each wave knocks the service down again just as it stands up."],
["p","And cap the total. A retry budget of roughly ten percent of first attempts, after which you fail fast. The budget is the part everyone leaves out, and it is precisely the part that turns retries from an amplifier into a safety net."],
["p","One more, easy to miss and expensive: retry at one layer only. If the SDK retries three times, and your service retries three times, and the gateway retries three times, then one user pressing a button once becomes twenty-seven attempts against a service that was already in trouble."],
D("Timeouts — how long to wait before giving up",{say:"Every outgoing call has its own timeout, set from that dependency's normal slow case plus a margin. A hundred and fifty milliseconds for the fraud check, five seconds for the card capture. There is no single global value, because those are not the same thing.",
use:["Every call. A dependency with no timeout can hold a request open until the user gives up."],
avoid:["One value applied everywhere. It will be far too short for the slow dependency and far too long for the fast one."],
consider:["A short timeout protects you and creates duplicate work, because you abandon calls that were about to succeed. A long one protects the other service and lets delays accumulate in yours.","Pass the remaining time down the chain, so a service does not start work that its caller has already stopped waiting for.","The real cost is not the waiting. A blocked request holds a thread and a connection, so waiting thirty seconds removes capacity from everyone else for thirty seconds."]}),
D("Retries — with backoff, randomness, and a hard cap",{say:"Three attempts, starting at two hundred milliseconds and doubling to a two-second ceiling, each delay randomised. Only on timeouts and overload responses, only with an idempotency key, and capped so retries never exceed ten percent of first attempts.",
use:["Failures that are genuinely temporary, on operations that are safe to repeat."],
avoid:["Client errors and declined cards. No number of attempts changes a decline.","Anything not safe to repeat without a key protecting it.","A dependency that is failing because it is overloaded, where retrying is what keeps it down."],
consider:["Increasing delays spread attempts out. Randomising them breaks up the synchronised waves that a thousand clients failing together would otherwise produce, each wave knocking the service over as it recovers.","Cap the total. Once retries exceed roughly a tenth of normal traffic, fail fast instead. This is the part people leave out, and it is what turns retries from an amplifier into a safety net.","Retry at one layer only. Three retries in the client library, three in your service and three at the gateway is twenty-seven attempts from one button press."],
fails:"A dependency starts failing half its calls. Every client retries three times, so its incoming load triples at the exact moment it can least handle it. The retries are not helping it recover — they are the reason it cannot.",
cases:["A payment capture is retried with its key.","A fraud score is not retried at all, because latency matters more and there is a fallback.","An email is retried from a queue rather than inline, so nobody waits for it."]}),
["ex","Payment capture retry policy\nattempts 3 · base 200 ms · ×2 · cap 2 s · full jitter\nretry on: timeout, connection reset, 503\nnever on: 4xx, declined\nrequires: Idempotency-Key\nbudget:   10% of first attempts"],
["t","Retries improve success under blips and worsen outages that are load-related. The budget is what lets you have the first without the second."]
]},
{id:"isolation",title:"Circuit breakers, bulkheads, load shedding, degradation",blocks:[
["p","Three mechanisms, and the useful way to hold them in your head is by who each one protects."],
["p","A circuit breaker protects the caller. Recommendations is down, and every call to it burns the full timeout before failing. Your checkout threads are now spending five seconds each waiting for an answer you were going to render as an empty row anyway. After twenty consecutive failures the breaker opens: stop calling, fail instantly, draw the page without that row. Every thirty seconds, let a single request through to see whether it has recovered."],
["p","It protects the dependency too, by giving it quiet in which to recover rather than a wall of traffic the instant it comes back up."],
["p","A bulkhead protects your dependencies from each other. Each gets its own connection pool and concurrency limit, so a slow recommendation service exhausts its own compartment while payments carries on. Share one pool between them and a single slow dependency consumes every thread in it, taking down calls that had nothing to do with it."],
["p","Load shedding protects the callee, which in this case is you. Above roughly ninety percent saturation, start refusing low-priority work with a cheap 503 and a Retry-After. Analytics and recommendation refreshes go first. Checkout is never shed."],
["p","The arithmetic here is stark and worth saying aloud. A rejected request costs microseconds. A request you accept and then fail to serve costs a thread for the entire timeout. Under overload, refusing work quickly is how you keep serving the work you already accepted — and autoscaling is far too slow to do this job for you."],
["p","Graceful degradation is the same decision expressed to the user. If the tax service is down, show an estimate and label it. If recommendations are down, the row is simply absent and nobody notices. If payments is down, you are down — because a checkout that silently guesses at a tax total is considerably worse than an honest error."],
["p","Decide which dependencies are essential and which are optional now, in writing. You will not be making good judgements about it at two in the morning on Black Friday."],
D("Circuit breaker — stop calling something that is clearly down",{say:"Recommendations gets a breaker. After twenty consecutive failures we stop calling for thirty seconds, render the page without that section, then let one request through to test whether it has recovered.",
use:["Calls you wait on, to a dependency that tends to fail as a whole, where you have something sensible to show instead."],
avoid:["Dependencies with no sensible fallback, where opening the breaker just changes how you fail.","Work driven by messages, where moving the message aside is the better answer."],
consider:["Three states: closed and calling normally, open and failing immediately, and half-open where a single request is allowed through to test recovery.","Track it per dependency rather than globally, so one failing service does not disable calls to a healthy one.","It protects you from wasting threads, and it protects them by giving them quiet instead of a wall of traffic the moment they recover."],
fails:"Too sensitive and it flips open and closed repeatedly. With no test request it never closes again. And tracking it per machine when the failure is service-wide means every machine has to learn the same lesson separately."}),
D("Bulkhead — stop one slow dependency from consuming everything",{say:"Each dependency gets its own connection pool and its own limit on concurrent calls, so a slow recommendations service fills up its own allocation while payments carries on untouched.",
use:["Any service with several dependencies that would otherwise share one pool of threads or connections."],
avoid:["Nothing much — though capacity reserved for one dependency does sit idle when that dependency is quiet."],
consider:["The name comes from ships, where sealed compartments stop one breach from flooding the whole hull.","Without it, one slow dependency consumes every thread and takes down calls that had nothing to do with it."]}),
D("Load shedding — refuse work early when you are overloaded",{say:"Above ninety percent saturation the gateway rejects analytics and recommendation refreshes immediately, with a header saying when to retry. Checkout is never shed.",
use:["Every service, because every service can be overloaded. The edge most of all."],
avoid:["Shedding after the expensive work is already done. Reject before you spend anything.","Shedding without priorities. Browsing should be dropped long before checkout."],
consider:["Trigger on how much work is queued or how slow responses have become, rather than processor usage alone, which lags.","A rejected request costs microseconds. A request you accept and then fail to serve costs a thread for the whole timeout. Under overload, refusing quickly is how you keep serving what you accepted.","Autoscaling cannot do this job. It reacts in minutes and this decision is needed in milliseconds."]}),
D("Degradation — decide in advance what may disappear",{say:"If tax is unavailable we show an estimate and say so. If recommendations are unavailable the section is simply absent. If payments is unavailable we are down, because a checkout that quietly guesses a tax total is worse than an honest error.",
use:["Every dependency that is not essential."],
avoid:["Silently falling back on something essential. A wrong total presented confidently is worse than a failure."],
consider:["Sort dependencies into essential and optional now, in writing. You will not make good judgements about it during an incident.","This is the one of the three that the user actually experiences, which is why it is a product decision as much as an engineering one."]}),
["t","Breakers and bulkheads protect the caller; shedding protects the callee; degradation protects the user. A complete design has all three and says which features may vanish under stress."]
]},
{id:"cascades",title:"Cascading failures",blocks:[
["p","Here is how a site goes down on Black Friday without a single component failing."],
["ex","Black Friday 09:00\nOne server GC-pauses, fails health check, evicted.\n9 servers at 85% → 95%. Two slow, fail checks, evicted.\n7 servers > 100%. Ninety seconds later: all evicted, all restored,\nall evicted again. Checkout down. No hardware failed. No bug."],
["p","Read that again and notice what actually broke. Nothing. No hardware fault, no bug, no bad deploy. One garbage collection pause, amplified by mechanisms that were each individually sensible."],
["p","That is the defining property of a cascade. Every mechanism that helps you survive a small failure — retry, reroute, restart, evict — amplifies a large one. The behaviours are not wrong. They are correct at one scale and catastrophic at another, and nothing in the system knows which scale it is currently in."],
["p","Which is why the brakes have to exist before the event. Shed load before you are saturated rather than after. Cap retries with a budget. Run with enough headroom to lose your largest failure domain at peak traffic. Bring recovered servers back gradually instead of at full load, because a cold server given full traffic fails immediately and rejoins the cascade."],
["p","And keep manual brakes within reach, because you will want them: pause the balancer's evictions, block the client retrying hardest, switch off the optional feature. Knowing in advance which switches exist is the difference between a ten-minute incident and a two-hour one."],
D("Cascading failure — when the safety mechanisms cause the outage",{say:"Shed load before saturation, cap retries with a budget, keep enough headroom to lose the largest failure domain at peak, return recovered servers to traffic gradually, and make health checks tell busy apart from dead.",
use:["Design all of this before the event, because none of it can be added during one."],
consider:["The things that feed a cascade: retries, evicting servers for being slow, cold caches, autoscaling that reacts too late, long synchronous chains without timeouts, and sending full traffic to a machine that just started.","Keep manual controls ready: pause evictions, block the client retrying hardest, switch off the optional feature. Knowing which switches exist is the difference between a ten-minute incident and a two-hour one."],
fails:"Every mechanism that helps you survive a small failure — retry, reroute, restart, evict — makes a large one worse. They are correct at one scale and catastrophic at another, and nothing in the system knows which scale it is currently operating at."}),
["t","The goal isn't to eliminate failure; it's to make the system fail in a bounded, recoverable way rather than a self-amplifying one."]
]},
{id:"recovery",title:"Redundancy, RPO/RTO, zones and backups",blocks:[
["p","Finally, the two questions that decide how much redundancy you actually buy. They are answered per component, never once for the whole company."],
["p","RPO is how much data you can afford to lose. RTO is how long you can afford to be down. Write both down for each system and the strategy falls out on its own."],
["p","For the orders database RPO is zero — you cannot lose an order somebody has paid for — and that single number forces a synchronous replica, which costs a few milliseconds on every write forever. For the session cache RPO is effectively infinite: users log in again and are mildly irritated. For analytics, twenty-four hours from last night's snapshot is completely fine."],
["p","Three answers, three different architectures, three very different bills. The mistake is choosing one number for everything, which is either far more expensive than it needed to be or quietly not good enough where it mattered."],
["h","A replica is not a backup"],
["p","One distinction that costs companies dearly, usually once."],
["p","A replica survives a machine dying. It also replicates a mistaken DELETE perfectly, instantly, to every copy you own. The thing that saves you from corruption and from human error is a backup — an old copy that deliberately does not track the present."],
["p","So you need both. And you need to actually restore from a backup on a schedule, because a backup nobody has restored is a hope rather than a plan. The same is true of a standby nobody has ever failed over to. The only way to know either of them works is to have used it recently, on purpose, when nothing was wrong."],
D("How much data you can lose, and how long you can be down",{say:"The orders database can lose nothing and must be back within a minute, which means a synchronous copy and automatic failover. The session cache can lose everything, because users log in again. Analytics can lose a day, from last night's snapshot.",
use:["State both numbers per system. They choose the redundancy strategy for you."],
avoid:["One pair of numbers for the whole company. It will be far too expensive somewhere and quietly inadequate somewhere else."],
consider:["How much you can lose decides how often and how synchronously you copy data.","How long you can be down decides whether failover is automatic or someone gets woken up."]}),
D("Zones always, regions only when justified",{say:"Every stateless tier runs across two availability zones, and the database keeps a synchronous copy in another zone. Spreading across regions only if the business genuinely cannot survive losing one, because it is a large step in cost and difficulty.",
use:["Multiple availability zones: always.","Multiple regions: only for a genuine global latency requirement or a disaster recovery requirement you can point to."],
consider:["Zones have independent power and network but are close enough to keep copies perfectly in step. That covers most real failures.","Regions are hundreds of miles apart, so copies are always behind, and every consistency question becomes hard."]}),
D("Copies and backups are not the same thing",{say:"A live copy survives hardware failure instantly. It also copies a mistaken delete perfectly, to every copy, immediately. Backups survive mistakes and corruption, slowly. We need both, and we restore from a backup every month to prove it works.",
use:["Every primary datastore."],
avoid:["Treating live copies as backups. They replicate your errors as faithfully as your data."],
consider:["A standby you have never failed over to is a hope rather than a plan. The only way to know it works is to have used it deliberately when nothing was wrong.","Walk the diagram and ask what happens when each box dies: the balancer, the primary database, the cache, the message broker, the domain name service, and the certificate that expires on a Sunday."]}),
["t","Each nine costs roughly 10× the last. Spend redundancy on components whose failure stops revenue; write down lower targets, honestly, for the rest."]
]}
]};
