const D = (name, o) => ["dec", { name, ...o }];
export default {id:"patterns",num:"11",title:"Specialised design patterns",example:"a Snapchat-style media app",
tagline:"Feeds, chat, media, search, location, payments, counters, scheduling, ML — each pattern as a decision: use when, don't, alternative, tradeoff, failure.",
terms:["Hybrid fan-out","Presence","Presigned upload","Adaptive bitrate","Inverted index","Geohash","Ledger","HyperLogLog","Feature store","Canary"],
objectives:["See the problem shape inside a product prompt: contention, fan-out, large objects, ordering, search, async","Choose push, pull or hybrid fan-out and set the threshold","Design a media pipeline from presigned upload to CDN","Choose the index that turns a hard query into a prefix or key lookup","Draw an ML serving system as a dependency with a p99 and a fallback"],
sections:[
{id:"feeds",title:"Feeds: fan-out on write, on read, or hybrid",blocks:[
["p","A user opens the app and sees stories from the people they follow. How did that list get there?"],
["p","There are only two moments when the work can be done: when the post is written, or when the feed is read. Everything else is a blend of the two."],
["p","Fan-out on write does it at post time. When you post, the system pushes the post id into the cached timeline of each of your followers. Reading a feed is then a single fetch of an already-assembled list, which is why good feeds feel instant."],
["p","The cost is write amplification. One post becomes one small cache write per follower. For an account with two hundred followers that is two hundred writes and completely fine."],
["p","Now an account with fifty million followers posts. That is fifty million cache writes for one action. During a live event, with several such accounts posting at once, the fan-out workers become the bottleneck for the entire product."],
["p","Fan-out on read does the opposite. Store the post once, and assemble each feed at read time by fetching recent posts from everyone the reader follows. Writes become trivial. Reads now touch hundreds of accounts, and you redo that work every single time the user pulls to refresh."],
["p","Neither is right on its own, because the distribution is the actual problem: almost every account is small and a handful are enormous. So do both. Push for ordinary accounts, pull for the few above a threshold, and merge the two lists when the feed is read."],
["p","The threshold is a tuning knob rather than a constant, and an account crosses it in the middle of the afternoon on the day it goes viral. Mentioning that case before you are asked about it is worth doing."],
["p","One detail that decides whether the product feels right: insert the author's own post into their own timeline synchronously. Everybody else can wait a few seconds. The person who just posted must see it immediately, or they will post again. That is read-your-writes from Chapter 04, applied to exactly one reader."],
D("Push — build every follower's feed when the post is made",{say:"For ordinary accounts, each new post is written into every follower's stored feed as it happens. Reading a feed is then a single fetch of a list that already exists, which is why feeds feel instant.",
use:["Networks where most people follow a manageable number of accounts and reads must be fast."],
avoid:["Accounts with millions of followers. One post becomes millions of small writes, and during a busy period those writes become the bottleneck for the whole product."],
consider:["Keep each stored feed to a fixed length, a few hundred entries, and rebuild it from the follow graph if someone scrolls past the end.","The write cost is exactly the follower count, which is fine for the long tail and impossible for the head."]}),
D("Pull — build the feed when it is read",{say:"For the handful of very large accounts we store the post once and gather their recent posts at read time, merging them into the feed. One write regardless of how many people follow them.",
use:["Accounts with enormous follower counts.","Feeds that are ranked rather than chronological, where you were going to gather candidates anyway."],
avoid:["Using it for everything, because then every feed open reads from hundreds of accounts and repeats that work on every refresh."],
consider:["Read time grows with how many accounts someone follows, and the same work is redone every time they look."]}),
D("Hybrid — push for most accounts, pull for the large ones",{say:"Push for anyone under ten thousand followers, pull the few large accounts a reader follows at read time, then merge and rank. Expired stories are filtered out when read rather than deleted from millions of feeds.",
use:["Any real network, because the distribution is always a long tail plus a small number of enormous accounts."],
avoid:["Small networks where pushing to everyone is simply fine."],
consider:["The threshold is a setting you tune, not a constant, and an account crosses it in the middle of the afternoon on the day it goes viral. Mention that before you are asked.","The author must see their own post immediately, so insert it into their own feed synchronously while everyone else waits for the background work."],
fails:"If the store holding feeds loses a machine, every feed on it must be rebuilt at once. Rate-limit those rebuilds. Set the threshold too high and writes overwhelm you; too low and reads get slow.",
cases:["A large account posting during a live event, which goes down the pull path with its posts cached.","Someone unfollows an account, so filter that account out when reading rather than rewriting their feed.","A story expires but is still in the stored feed, so check the expiry when reading."]})
]},
{id:"chat",title:"Chat: transport, ordering, delivery, presence",blocks:[
["p","Two messages arrive at the recipient's phone in the wrong order. Whose clock was wrong?"],
["p","Both, probably, and it does not matter — because you should never have been ordering by clocks in the first place. Phone clocks drift, users set them by hand, and a message composed offline on a plane can carry a timestamp from last week."],
["p","Ordering comes from the server. When it accepts a message it assigns the next sequence number for that conversation. Clients render by sequence, and because sequence numbers are contiguous, a client can notice a gap and ask for what it missed."],
["p","Per conversation, not globally — the same scoping decision as partitions in Chapter 07. A conversation is small, so one sequencer per conversation costs nothing, while a single global sequencer would be a bottleneck for the whole product and would buy an ordering guarantee nobody asked for."],
["p","Then delivery, where there is one rule: store durably before acknowledging, always. If you push to the recipient's connection first and persist afterwards, a gateway crash in the gap loses a message the sender was already told had been sent. Losing a message that displays as sent is the one unforgivable bug in a messenger."],
["p","Offline is not an exceptional case; it is most of the time. So a device syncs from its own last-seen sequence when it reconnects, and the push notification exists only to wake the app so it can do that. Three devices means three independent positions, each filling its own gap."],
["p","And a group of fifty thousand is a fan-out problem wearing a chat costume: fifty thousand deliveries for one message, performed by workers after the sender has been acknowledged, never on the sender's critical path."],
D("Sequence numbers — the server decides the order",{say:"When the server accepts a message it assigns the next number for that conversation. Clients display messages in that order and can tell when one is missing, because the numbers would have a gap.",
use:["Putting messages within one conversation into a definite order."],
avoid:["Ordering by timestamps. Phone clocks drift, people set them by hand, and a message written offline can carry a time from last week.","Numbering every message in the entire system. One counter for everything is a bottleneck, and nobody needed that guarantee."],
consider:["A conversation is small, so one counter per conversation costs almost nothing and is never contended."]}),
D("Store it durably, then deliver it",{say:"The message is written to storage before the sender is told it was sent, then pushed to whichever servers hold the recipient's devices. If nobody is connected, a notification wakes the app, which then catches up from its own last-seen position.",
use:["Any messaging system."],
avoid:["Pushing before storing. A crash in that gap loses a message the sender has already been told was delivered, which is the one unforgivable bug in a messenger."],
consider:["Each of a user's devices tracks its own position, so each fills its own gap independently.","Delivered and read receipts are just small messages travelling in the other direction.","A group of fifty thousand becomes fifty thousand deliveries. Do that in background workers after the sender has been acknowledged, never on the sender's critical path."],
cases:["Someone is offline for a week, and their phone catches up from the server when it reconnects.","The same message is sent twice because of a retry, so the client discards it by its identifier.","Two devices are at different positions, and each requests only what it is missing."]}),
D("Presence — knowing which server holds which user",{say:"A presence service maps each user to the servers currently holding their connections. Those servers hold nothing durable — only the live connections — so losing one costs reconnections rather than data.",
use:["Any product built on long-lived connections."],
consider:["Heartbeats detect connections that have silently died. Give last-seen a grace period, or people flicker offline every time a train enters a tunnel.","Servers holding connections need a way to pass messages to each other, since the sender and recipient are rarely on the same one."]}),
D("Concurrent edits to the same text",{say:"Two people edit the same caption at the same moment. Either the server rewrites each edit so it still makes sense after the other, or the data structure is designed so that applying edits in any order reaches the same result. I would name the approach and not derive it on a whiteboard.",
use:["Documents several people edit simultaneously."],
avoid:["Keeping whichever edit arrived last. On text that silently deletes somebody's work."],
consider:["Rewriting edits requires a central server to decide the order. The self-merging data structures do not, but they carry extra bookkeeping that grows with the edit history."]})
]},
{id:"media",title:"Media: upload, process, deliver",blocks:[
["p","A video file is fifty megabytes. Where do the bytes actually go?"],
["p","Not through your API servers. If they do, then an application tier you sized for small JSON requests is now a file transfer service, and a single upload occupies a request handler for the entire length of a phone's slow upstream connection."],
["p","Instead the client asks your API for a presigned URL and uploads directly to object storage. Your servers never touch a byte — they grant permission and later hear about the outcome. Multipart upload means a dropped connection resumes from the failed part rather than from the beginning, which on a mobile network is the difference between a feature that works and one that does not."],
["p","Processing then happens asynchronously, through the pipeline from Chapter 07, and the video carries a status while it is under way."],
["p","The subtlety worth stating is how publishing works. Six renditions are produced independently and finish at different times. If the video becomes readable as each one lands, some viewers get a broken player. So write every output to a content-addressed path and publish by flipping a single pointer once the whole set exists. The video is either the old version or the new one, and never a mixture of both."],
["p","Ephemerality, incidentally, is a storage strategy and not merely a product feature. Fifty million videos a day at eight megabytes each is four hundred terabytes a day of originals. Retained forever that is unaffordable. Expired after thirty days it is a manageable steady state. The product decision and the storage bill turn out to be the same decision."],
["tbl",{cols:["Choice","Use when","Tradeoff"],rows:[["Database storage","Tiny transactional files only","Convenient, expensive, unscalable"],["Object storage","Images, video, large files","Cheap and durable; metadata elsewhere"],["Presigned direct upload","Anything over a few MB","Zero bytes through app servers; validate after"],["Async transcoding","Multiple renditions needed","Fast upload; eventually available"],["CDN + adaptive bitrate","Global playback","Instant start on bad networks; purge/egress cost"]]}],
D("Upload straight to storage, in resumable pieces",{say:"The client asks our API for a temporary signed URL and uploads directly to object storage in parts, so a dropped connection resumes from the failed part. Our servers never handle a single byte. Storage then notifies us that the upload finished.",
use:["Any upload beyond a few megabytes."],
avoid:["Passing the bytes through your application servers. You sized those for small requests, and now one upload occupies a request handler for the length of a phone's slow connection."],
consider:["Check the file type, size and safety after upload and before publishing. A signed URL means the client chose what to send.","Make the URLs short-lived and valid for exactly one destination, or a leaked one becomes free storage for a stranger."],
fails:"Unchecked files reach viewers. Or a long-lived signed URL is shared and used by people you never authorised."}),
D("Process in the background, publish atomically",{say:"An event triggers workers to scan the file, produce six versions, generate a thumbnail and check it for policy. Each writes to a location computed from the video id, so repeating a step is harmless. The video becomes visible when one pointer is flipped.",
use:["Anything needing several output formats, or a moderation step before publication."],
avoid:["Doing this inside the upload request. It takes minutes and the request cannot."],
consider:["Produce the common formats immediately and the rare ones on first request, which saves a great deal of computation.","Publishing must be a single pointer change once every output exists. Otherwise some viewers get a half-finished set and a broken player."]}),
["ex","50 M videos/day × 8 MB = 400 TB/day originals · ×1.6 renditions ≈ 640 TB/day\n×365 ≈ 230 PB/year before expiry. Expire at 30 days → ~20 PB live.\nEphemerality is a storage strategy."]
]},
{id:"search-location",title:"Search and location: turning hard queries into index lookups",blocks:[
["p","Two problems that look entirely different and are the same idea underneath: turn a hard query into a cheap lookup by building the right index for it."],
["p","Full-text search first. You cannot answer which documents contain the word sunset by scanning documents, because that is linear work per query over your whole corpus. So invert the relationship: build a map from each term to the list of documents containing it. Now the query is a lookup and an intersection of a few lists."],
["p","That index is fed by change events, which means it is always slightly behind the source of truth. Say so explicitly: rename yourself and search immediately, and you may not find yourself for a few seconds. It is also never the source of truth — losing the index should mean rebuilding it, never losing data."],
["p","Location second. Find friends within two kilometres. Latitude and longitude are two dimensions, and an ordinary index sorts on one, so a range query on both is close to useless: it finds a band of the planet and then filters most of it away."],
["p","Geohashing solves this by folding two dimensions into one string in which nearby places share a leading prefix. Now nearby is a prefix match, and any key-value store on earth can do that quickly."],
["p","The trap is boundaries. Two points can be metres apart and still fall into different cells with different prefixes, so you always query the cell plus its eight neighbours. Anyone who forgets this ships an app where friends across the street are invisible and nobody can work out why."],
["p","And matching a rider to a driver is this plus Chapter 04's contention problem stacked together: find nearby drivers, then assign exactly one — with a conditional write, so a driver offered two rides can accept only one, and a lease, so an unresponsive driver's offer expires and moves on to the next."],
D("Inverted index — from word to the list of documents containing it",{say:"Search runs on a separate index built from change events: for each word, the list of documents containing it. A query becomes a lookup and an intersection, then ranking. It is always slightly behind the real data, and I would say by how much.",
use:["Free text search, ranking, filters and autocomplete."],
avoid:["Treating it as the source of truth. Losing it should mean rebuilding it, never losing data.","Searching with a wildcard against the database, which reads every row for every query."],
consider:["Split the index by document and every machine answers part of every query. Split it by word and each query touches fewer machines but popular words become hot.","Autocomplete is a different structure: a prefix tree built from what people actually searched for, held in memory.","Handle misspellings by indexing fragments of words, or by accepting near-matches within a small edit distance."],
fails:"Change your name, search for yourself immediately, and you are not there for a few seconds. That is inherent, so state it rather than being caught by it."}),
D("Turning a map position into a lookup key",{say:"A geohash converts latitude and longitude into a string where nearby places share a leading prefix, so within two kilometres becomes a prefix match on one cell plus its eight neighbours. Any key-value store can answer that quickly.",
use:["Finding things nearby: friends, drivers, restaurants."],
avoid:["Range queries on latitude and longitude in an ordinary index. Those sort on one dimension, so you get a band across the planet and then filter almost all of it away."],
consider:["Choose the cell size from the search radius you actually need.","Always query the neighbouring cells too. Two points can be metres apart and fall either side of a cell boundary, and forgetting this ships an app where friends across the street are invisible.","A quadtree adapts instead: small cells where it is crowded, one enormous cell over the ocean.","Twenty million location updates a second belongs in memory, split by region. Losing a few seconds of positions in a crash is perfectly acceptable."],
cases:["Matching a rider to a driver is this plus a contention problem: find who is nearby, then assign exactly one with a conditional write and a time limit, so an unresponsive driver's offer expires and moves on."]})
]},
{id:"payments-counters",title:"Payments and counters",blocks:[
["p","Money gets its own data model, and the rule is short: never store a balance you can edit."],
["p","A mutable balance column cannot be audited. When it is wrong — and one day it will be — there is no way to discover when it became wrong, or why, or what it should have been."],
["p","So record the movements instead, as immutable paired entries that always sum to zero, and derive the balance from them. Corrections are new reversing entries, never edits to old ones. The balance is now reproducible from history, and reconciliation against the payment processor becomes possible because there is finally a history to reconcile against."],
D("Ledger — record movements, never edit a balance",{say:"Every payment is two matching immutable entries that sum to zero. The balance is derived from those entries rather than stored and edited. Corrections are new reversing entries, and we compare against the payment provider's records nightly.",
use:["Anything moving money or credits."],
avoid:["A balance column you update. When it is wrong, and one day it will be, there is no way to find out when or why."],
consider:["Nothing is ever updated or deleted. To correct a mistake you add an entry that cancels the previous one, so the history stays complete.","Every operation that changes money needs the safe-retry key from Chapter 02, multi-step flows need the pattern from Chapter 05, and strong consistency belongs where money moves — receipts and analytics can lag.","Fraud scoring is a service you call with a timeout and a cautious answer if it does not respond."],
fails:"The stored balance drifts away from the sum of the entries. Have a job that recalculates and alerts, because silent drift in a financial system is the worst possible failure."}),
["p","Counters are the same question with the opposite answer, and the contrast is the lesson."],
["p","A view counter implemented as an incrementing database row is the hottest row in the system: every view is a write to one place, and a viral video turns it into a hot partition of exactly one. But nobody can tell 1,203,417 from 1,190,000, and no decision anybody makes depends on the difference."],
["p","So spread the counter across shards, buffer in memory, flush periodically, and accept a number that is a few seconds stale and slightly approximate. For distinct viewers, HyperLogLog gives a count within about one percent using kilobytes, instead of storing a set containing every viewer id that ever watched."],
["p","The rule separating the two cases is worth memorising: exact where somebody is billed, approximate where somebody merely glances. Applying the ledger's rigour to a view counter melts your database. Applying the counter's looseness to money is fraud."],
["tbl",{cols:["Counter","Use when","Tradeoff"],rows:[["Exact row increment","Billing, inventory","Correct; hottest row in the DB"],["Sharded / buffered counter","Displayed counts at high rate","Cheap; seconds stale"],["HyperLogLog","Distinct viewers","KBs of memory; ~1% error"],["Count-Min Sketch","Frequencies / top-K","Tiny; approximate"],["Stream processing","Seconds-fresh results","Approximate, complex"],["Batch","Exact, complete","Hours late"]]}],
D("Approximate counters — close enough for anything people just look at",{say:"View counts are spread across several keys and written out periodically. Distinct viewers are estimated with a structure that uses kilobytes instead of storing every viewer id. Nobody can tell 1,203,417 from 1,190,000; billing gets its exact numbers from a separate batch job.",
use:["Any number a user simply glances at."],
avoid:["Anything a customer is charged for."],
consider:["The rule is exact where somebody is billed, approximate where somebody merely looks. Applying ledger rigour to a view counter melts your database; applying counter looseness to money is fraud.","One common arrangement computes an approximate answer immediately and corrects it later with a slower exact job. Another simply reprocesses the event log when the number needs to be right."]})
]},
{id:"scheduling-ml",title:"Scheduling and production ML",blocks:[
["p","Two more, briefly, because both come up constantly and both are usually hand-waved."],
["p","Scheduling first. Stories expire after twenty-four hours, and something has to make that happen for millions of them. Not a cron box — that is one machine, one failure, and no safe way to run a second one alongside it."],
["p","Store jobs in a time-indexed table, have workers claim due jobs with a lease, and use the fencing tokens from Chapter 05 so that a paused worker cannot act on a claim that has already expired. Then, because duplicates remain possible no matter what you do, make every job body idempotent. You will notice that is the same conclusion as Chapter 07, reached by a different road."],
D("Running jobs on time, across many machines",{say:"Jobs are stored with the time they are due. Workers claim due jobs with a time-limited hold that carries an increasing number, so a worker that froze cannot act on an expired claim. Because duplicates are still possible, every job is safe to run twice.",
use:["Expiring stories, reminders, delayed work, retries with increasing delays, and scheduled jobs generally."],
avoid:["A single machine running scheduled tasks. That is one thing that can fail, and you cannot safely run a second copy of it."],
consider:["The hold and its increasing number are the mechanism from Chapter 05, and they are what make this safe rather than merely likely to work.","Limit how many times a job is attempted, then set it aside for a person to look at."]}),
["p","And machine learning, which candidates tend to draw as a box labelled ML and then walk past."],
["p","Treat it as a dependency like any other: it has a p99, an error rate, a deployment process and a fallback. If the ranking model is slow you serve the previous model or a simple heuristic. You do not block the feed on it, any more than you would block checkout on recommendations."],
["p","The failure mode worth knowing is training-serving skew. The model was trained on features computed one way — offline, in a batch job, in one language. At serving time those same features are computed by different code, under a latency budget, possibly in a different language. When the two computations disagree even slightly, the model evaluates beautifully and performs poorly in production, and nothing anywhere raises an error."],
["p","A feature store exists to make both paths use one definition, which is why it appears in every serious production ML system and in almost no tutorials."],
["p","And roll models out the way you roll out code: shadow first, scoring without acting, then a small canary, then promote — keeping the previous version as a rollback target, because you will need it."],
D("A model is a dependency, not a magic box",{say:"The ranking model is a service with a response time, an error rate and a deployment process. Requests are grouped together to keep the hardware busy, it scales on how much work is queued, and if it is slow we fall back to the previous version or a simple rule.",
use:["Any prediction made while a user is waiting."],
avoid:["Drawing it as a box labelled ML and moving on. It has all the failure modes of any other service plus several of its own."],
consider:["Training happens offline and produces a versioned artefact you can roll back to. Serving is a separate system that loads it.","Some predictions can be computed overnight in bulk, such as friend suggestions. Others must happen as the request arrives, such as moderation.","Features must be computed identically during training and during serving. When those two paths are different code, the model performs well in testing and badly in production, and nothing raises an error. A shared feature definition is what prevents it.","Roll out by scoring without acting, then a small percentage of real traffic, then fully.","Watch for the input data drifting away from what the model was trained on, and hold back a slice of random traffic so the model is not only ever learning from its own recommendations."],
fails:"It works in evaluation and fails in production because features are computed differently. A slow model blocks every request because nobody set a timeout. Or a ranking model trained on clicks it caused itself, which reinforces whatever it happened to show first."}),
["t","Every famous system is a few problem shapes in a costume. When you get a novel prompt, ask which shapes it contains — contention, fan-out, large object, ordering, search, async — before you draw anything."]
]}
]};
