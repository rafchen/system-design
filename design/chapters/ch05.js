const D = (name, o) => ["dec", { name, ...o }];
export default {id:"foundations",num:"05",title:"Core foundations",example:"a URL shortener",
tagline:"Protocols, transport and the performance vocabulary — as decisions, not definitions. Each one: what to say, when to use it, when not to, what it costs, how it fails.",
terms:["REST vs GraphQL vs gRPC","WebSockets vs SSE","TCP vs UDP","HTTP/2 vs HTTP/3","Connection pooling","Blocking vs nonblocking","p99 latency"],
objectives:["Pick a communication protocol from the traffic shape and justify it in one sentence","Pick a real-time mechanism by asking who initiates and how often","Explain why a persistent connection makes the connection server stateful but not the application","Choose TCP or UDP from whether late data still has value","Use latency, throughput, availability and percentiles as decision inputs, not vocabulary"],
sections:[
{id:"protocol",title:"Choosing the API protocol: REST, GraphQL or gRPC",blocks:[
["p","A URL shortener is about as small as a useful system gets. Someone hands you a long URL and gets back a short code. Someone else follows that code and lands on the original page. Two operations. That is the whole product, and it is small enough that every decision in this chapter is visible in it."],
["p","Start with the one that sounds like it wants a fashionable answer: how should the client talk to your server?"],
["p","It does not want a fashionable answer. Ask two duller questions instead. Who is the client — a browser, a phone, or another service you control and deploy? And does the client need to choose the shape of the response, or is one shape right for everybody?"],
["p","For the shortener both answers are easy. The client is a browser or a script, so it is not something you control or can upgrade. And there is exactly one shape a redirect can take. Two answers, and the protocol falls out: REST."],
["p","Now change the system and watch the answers change with it. A profile screen in a mobile app needs the user, their last ten posts, the comment count on each, and the avatar of whoever commented most recently. Over REST that is four or five round trips, and on a phone the round trips — not the bytes — are what the user feels. Here the client genuinely does need to choose the shape of the response, and the answer moves to GraphQL."],
["p","Change it once more. Your API server needs a fraud score before it will issue a code for a submitted URL. Both ends of that call are yours. Nobody outside the company will ever make it. It happens on every single write. You are now paying to serialise JSON and parse it back thousands of times a second in exchange for nothing, and the answer moves to gRPC."],
["p","That is the whole method, and it is worth saying plainly because most candidates never do: REST is the default you move away from, not the one you move toward. If you cannot name the client need that made you leave, you have not made a decision. You have expressed a preference."],
D("REST — resources named by URL, actions by HTTP verb",{say:"I would use REST here because the API is about things — links, users, orders — rather than actions, it is used by clients I do not control, and every proxy, browser and cache already understands it without being told anything.",
use:["Public APIs, or any API used by people outside your team.","Creating, reading, updating and deleting records that have a clear identity.","Web and mobile clients.","Responses that benefit from being cached by their URL, since the URL is the whole identity of the thing.","Simple request-and-response traffic, like the shortener's create and redirect."],
avoid:["Screens that need many related pieces of data at once and end up making six to ten separate requests to build one page.","Clients that each want a different subset of the same data.","High-volume calls between your own internal services, where the text format and the extra round trips cost more than they are worth."],
consider:["You will send fields nobody wanted, or force clients to make several calls to assemble one screen. Both are fixable — let clients ask for specific fields, or ask for related records to be included.","Put the version in the path, like /v1/, and only make additive changes within a version.","Status codes carry meaning and clients act on them: 201 means created, 409 means the request was fine but the world is not in the right state, 429 means slow down, 503 means try again later."],
nuance:"REST is not incapable of letting clients choose fields or include related records. GraphQL simply makes that the central idea rather than an add-on.",
alt:"GraphQL when clients need different shapes of the same data. gRPC for internal calls between services you own."}),
D("GraphQL — the client asks for exactly the fields it wants",{say:"I would consider GraphQL because the app needs different combinations of related data on different screens, and over a plain REST API that becomes several round trips per screen, which is what the user actually feels on a phone.",
use:["Several kinds of client — web, phone, television — each needing a different shape of the same data.","Deeply connected data, like a profile with posts, each post with comments, each comment with its author.","A front end that changes faster than the back end can add endpoints for it.","Cases where the number of round trips, rather than the amount of data, is the bottleneck."],
avoid:["Simple create-read-update-delete APIs, where it adds a whole query layer in exchange for nothing.","Public APIs, because you cannot control how expensive a stranger's query will be.","Responses you wanted the content delivery network to cache by URL."],
consider:["A single query can quietly trigger one database read per item returned — ask for fifty posts and get fifty separate author lookups. The fix is to batch those lookups, and you have to build it deliberately.","Someone can write a query nested twenty levels deep. Set limits on depth and on estimated cost, and reject queries that exceed them.","Permissions have to be checked per field rather than per endpoint, because one query can reach data that used to live behind several endpoints.","Caching is harder. Queries are sent as POST bodies, so there is no URL to cache by. You end up relying on saved named queries and client-side caches instead.","Rate limiting by number of requests stops meaning anything, because one request can be enormous. You have to limit by estimated cost."],
alt:"REST with parameters that let the client pick fields and include related records. Or a small dedicated API per client, tailored to that client's screens."}),
D("gRPC — typed, binary calls between services you own",{say:"I would use gRPC between our own internal services, because we control both ends, we want the contract checked at compile time rather than discovered in production, and at this call volume the cost of encoding and parsing text actually shows up in the profile.",
use:["Calls between services inside your own infrastructure.","Teams working in several languages, since the client code is generated from one shared definition.","Streaming, where either side may send a sequence of messages rather than one.","High call volumes, where the time spent turning objects into text and back is measurable."],
avoid:["Browsers. It can be made to work through a translating proxy, but it is never the direct path.","Public APIs, where being able to debug a call with ordinary command-line tools matters more than efficiency."],
consider:["The message format is defined in a schema file and sent as compact binary rather than text, which is where the efficiency comes from and why you cannot read it off the wire.","Set a deadline on every call and pass it down to whatever that call triggers. Without this, one slow service quietly holds up everything upstream of it.","Fields are identified by number, not name. Renumbering a field breaks every client that has not been rebuilt. Adding new fields is safe; reusing an old number is not."],
nuance:"The common arrangement is a browser talking REST or GraphQL to one gateway, and that gateway talking gRPC to everything behind it.",
cases:["The shortener uses REST everywhere. Two endpoints, public, cacheable, and no client needs a different shape.","A payments platform uses REST for its customers and gRPC between payments, fraud and ledger internally."]}),
["h","Why a network call is not a function call"],
["p","gRPC is built to make a remote call look like a local one. You call a method, you get a return value, and the network is hidden. That is the point of it, and it is also the one thing about it worth being nervous about."],
["p","A local function call has two outcomes: it returns, or your process has a much larger problem. A network call has four."],
["p","It succeeds and you hear so. It fails and you hear so. It succeeds and the response is lost on the way back — the work happened, and you have no idea. Or it is still running when you stop waiting, and it may finish correctly one second after you have already decided it did not."],
["p","The third and fourth have no equivalent inside a single process, and no amount of syntactic sugar removes them. A framework that makes the network invisible has not made it reliable; it has only made it easy to forget."],
["p","Chapter 11 is built entirely on those four outcomes. Notice them here, at the exact point where the abstraction first invites you to forget them."],
["t","REST is the safe default; you justify moving off it, never onto it. GraphQL trades server simplicity for client flexibility; gRPC trades debuggability for efficiency and contracts."]
]},
{id:"realtime",title:"Choosing the real-time mechanism: WebSockets, SSE or long polling",blocks:[
["p","The shortener needs none of this, and saying so out loud is worth a sentence in an interview. But add one feature — a dashboard where the owner of a link watches clicks arrive on it — and you have to decide how the browser finds out that something happened."],
["p","The instinct is to reach for WebSockets, because WebSockets are what real-time means to most people. Hold that instinct for one question: who actually needs to send?"],
["p","On the click dashboard, the server sends and the browser listens. The browser\'s only outbound traffic is the occasional ordinary request — change the date range, export a CSV. Frequent traffic one way, rare traffic the other. That is not a bidirectional problem, and buying a bidirectional mechanism means paying for connection management you did not need."],
["p","Now compare a chat window. The user types, the other user types, both sides send constantly, and neither is primarily a listener. There the sockets earn their cost."],
["p","So the test is not how live it feels. It is: does the client send often? If no, Server-Sent Events, which is a single long-lived HTTP response with automatic reconnection built into every browser. If yes, WebSockets. And if updates arrive every thirty seconds or slower, neither — poll, and spend the complexity somewhere it will be noticed."],
D("WebSockets — a connection either side can send on at any time",{say:"Both sides send frequently here, so a single long-lived connection beats repeatedly asking. I accept that the servers holding those connections now have something in memory that matters, so I will add a way to find which server is holding a given user.",
use:["Chat and messaging.","Multiplayer games.","Shared documents with live cursors.","Showing who is online and who is typing.","Anything where the client sends many small messages, not just the server."],
avoid:["Cases where the server pushes and the client only occasionally replies. Server-Sent Events does that with far less machinery.","Updates arriving every thirty seconds or slower. Just ask repeatedly.","Anything you wanted cached by URL at the edge."],
consider:["It starts as a normal HTTP request and then asks to be upgraded into a persistent connection, which is why it works through most existing infrastructure.","The load balancer has to keep sending a given connection to the same server, because that server is the one holding it.","Every deploy drops every connection. Let servers finish gracefully and have clients reconnect after a randomised delay.","Send a heartbeat periodically. A connection that has silently died looks exactly like an idle one until you try to use it.","Cap how many connections one server will hold, or one machine will accept far more than it can serve."],
cases:["A client disconnects and reconnects, and resumes from the last message number it saw.","Messages arrive twice or out of order, so each carries a sequence number and the client discards duplicates.","One user is connected on three devices, so a message must reach every server holding one of them.","The server holding a connection crashes. Clients reconnect elsewhere, which is safe because nothing durable was stored only on that server.","One user floods the connection with messages, so each connection gets its own rate limit."],
fails:"A server dies and a hundred thousand clients try to reconnect in the same second, which knocks over whichever server they land on. Randomise the reconnect delay. Idle connections also leak memory slowly, and some corporate networks silently close long-lived connections."}),
D("Server-Sent Events (SSE) — the server pushes, the client listens",{say:"The server sends and the client mostly listens, so I would use Server-Sent Events. It is an ordinary HTTP response that stays open, which gives us push without any of the connection management a two-way protocol needs.",
use:["Streaming a response as it is generated, a few words at a time.","Progress on a long-running job.","Live dashboards, notifications, prices and scores."],
avoid:["Cases where the client also needs to send frequently. Use WebSockets.","Binary data, since this carries text only."],
consider:["It is just a long-lived HTTP response, so it passes through existing infrastructure without special handling.","Browsers reconnect automatically and tell the server the last message they received, so resuming is built in rather than something you write.","The client sends in the other direction using ordinary requests, which is usually all it needs."],
nuance:"A job service typically uses both: POST /jobs creates the work, and GET /jobs/123/events streams its progress. Two mechanisms, one feature."}),
D("Long polling — the client holds a request open until there is news",{say:"Where persistent connections are not available, the client makes a request that the server simply does not answer until it has something to say, and then immediately makes another one.",
use:["Old clients, or networks that block newer mechanisms.","A fallback behind whatever you actually wanted to use."],
avoid:["Anything new, where Server-Sent Events or WebSockets are available."],
consider:["Every update costs a complete HTTP request and response, so the overhead per message is high.","How long the server holds the request is a direct trade against how quickly the client notices new data.","The server is holding thousands of open requests anyway, so you get the same memory pressure as a persistent connection with fewer of the benefits."]}),
["h","Important correction: 'stateful servers'"],
["p","A persistent connection means a particular connection server owns that live socket. It does not make the whole application stateful. Keep durable state in shared storage and let connection servers remember only which clients are attached."],
["ex","Client\n  ↓ WebSocket\nConnection server        (knows: which clients are attached to me)\n  ↓\nPub/sub or message broker (routes: message for user U → whichever\n                           connection servers hold U's devices)\n  ↓\nApplication services + database (own: users, messages, history)"],
["ul",["Connection-aware routing (which server holds user U?)","Pub/sub across connection servers","Heartbeats and reconnection logic","Graceful draining during deploys","A cap on connections per server"]],
["t","WebSockets buy bidirectional low latency and cost you connection management. SSE buys most of the benefit for server-push cases with none of the management. Choose SSE unless the client genuinely talks back often."]
]},
{id:"transport",title:"TCP or UDP, and which HTTP",blocks:[
["p","TCP or UDP is almost never a live decision in an interview. It is on every syllabus because it explains other decisions, and this is the section where that becomes visible."],
["p","One question settles it: if this piece of data arrives late, is it still worth having?"],
["p","A byte of a redirect response — obviously yes. A URL missing its last four characters is not a slightly worse URL, it is garbage. So TCP, which retransmits anything lost and holds everything behind it until the gap is filled."],
["p","A frame of a video call from four hundred milliseconds ago — no. Showing it now would be worse than showing nothing, because the conversation has moved on. So UDP, which sends and forgets and lets the application decide what late means."],
["p","That single test decides it every time, and it is the only part of this you need to carry into the room."],
["h","Where the tradeoff actually bites"],
["p","TCP\'s ordering guarantee is per connection and it is unconditional. If packet three of ten goes missing, packets four through ten may already be sitting in the receiver\'s kernel, complete and correct — and the application is not permitted to see any of them until packet three has been retransmitted and slotted into place."],
["p","That is head-of-line blocking, and it is not a bug. It is the guarantee working as specified. You asked for a byte stream in order; you are getting a byte stream in order."],
["p","It is also the reason HTTP/1.1 browsers open six connections instead of one, the reason HTTP/2 multiplexed many streams onto a single connection and still could not escape the problem underneath, and the reason HTTP/3 moved to QUIC over UDP and rebuilt reliability per stream instead of per connection."],
["p","Each generation is the same negotiation reopened: how much ordering do we really need, and across what scope? Hold that question. It comes back in Chapter 09, where a partitioned log gives you strict ordering inside a partition and flatly refuses it across partitions — for exactly this reason, and at exactly this cost."],
D("TCP — every byte arrives, in the order it was sent",{say:"This data has to arrive complete and in order, so TCP is worth the setup cost. It retransmits anything lost and hands the application a clean stream.",
use:["Every ordinary API call, page load and file transfer.","Anything where a missing or reordered byte is a bug rather than a minor glitch."],
avoid:["Live audio and video, where data that arrives late is worse than data that never arrives.","Tiny single exchanges where establishing the connection costs more than simply asking again, which is why DNS does not use it."],
consider:["Establishing a connection costs a round trip before any data moves, and adding encryption costs another.","If one packet is lost, everything sent after it waits — even though it has already arrived — until the missing one is resent. This is called head-of-line blocking, and it is the guarantee working as specified rather than a fault.","Its promise ends at the network layer. A write that TCP confirms was delivered is not a request that was processed, which is why you still need the safeguards in Chapter 06."]}),
D("UDP — send and forget; nothing is retransmitted",{say:"Late data has no value here, so we send each packet and never wait for a replacement. A video frame from half a second ago is worse than no frame at all.",
use:["Voice and video calls, and live game state.","Looking up domain names and synchronising clocks, where asking again is cheaper than a connection.","As the foundation for HTTP/3."],
avoid:["Anything that must arrive complete and in order."],
consider:["Whatever reliability you need, you build yourself on top — which is exactly what HTTP/3 does.","Firewalls and home routers handle it less gracefully, so some networks will block it."]}),
D("HTTP/1.1, HTTP/2 and HTTP/3 — three attempts at the same problem",{say:"HTTP/2 lets many requests share one connection, and HTTP/3 moves underneath to a transport where one lost packet no longer stalls the others. I would turn both on at the edge and not spend interview time on it.",
use:["HTTP/2 as the default for browsers and for gRPC.","HTTP/3 where connections are poor, which mostly means mobile networks."],
avoid:["Spending time here unless asked. This is a setting on your edge servers, not an architectural decision."],
consider:["HTTP/1.1 handles one request at a time per connection, which is why browsers open around six connections to the same site.","HTTP/2 carries many requests over a single connection, but they still share one ordered byte stream underneath — so one lost packet stalls all of them.","HTTP/3 gives each request its own independent stream, so a loss affecting one does not affect the others, and it establishes a connection in fewer round trips."]}),
D("TLS termination — where encrypted traffic gets decrypted",{say:"I would decrypt at the load balancer so certificates live in one place and the servers behind it do not each spend processing power on it. Then I would encrypt again for the internal hops, because the network you do not control is not only the public one.",
use:["Decrypting at the edge, for every public system.","Encrypting again between internal services when the data is sensitive or regulated."],
avoid:["Sending regulated data unencrypted inside your own network on the assumption that the network is safe."],
consider:["Whatever decrypts sees everything in plain text, which makes it the most security-sensitive component you run.","Certificates expire. Automate the renewal, or one will expire on a Sunday and nobody will be able to reach you."]}),
["t","For the shortener none of this is a decision: TCP, HTTPS, HTTP/2 at the edge. Say so in one sentence and move on. The one real choice on the redirect path is 301 vs 302 — see Chapter 18."]
]},
{id:"connections",title:"Connections, pooling and proxies",blocks:[
["p","Every arrow in your diagram is a connection to something, and connections are not free."],
["p","Opening a TCP connection costs a round trip. Adding TLS costs another. If your API server opens a fresh connection to the database for each redirect, the setup costs more than the query — you are spending a millisecond of handshake to buy a hundred microseconds of lookup."],
["p","So nobody does that. Every client keeps a pool of already-open connections and borrows one per query. The interesting question is not whether to pool. It is how big the pool should be, and the answer is emphatically not as big as possible."],
["p","Size it from the database\'s side rather than your own. A database has a level of concurrency it is happiest at, and past that point throughput does not plateau — it falls. More connections mean more context switching, more lock contention and more memory per connection, and less actual work finished. Fifty application servers each holding fifty connections is two and a half thousand connections arriving at a machine that was happiest with two hundred."],
["p","The failure this produces is worth memorising, because it is misdiagnosed constantly. One slow query starts holding connections. The pool drains. Now requests that would have taken a millisecond are queued waiting for a connection that does not exist. Every database dashboard looks healthy — low CPU, low disk, few queries — and every user sees a timeout."],
["p","The database really is fine. The queue in front of it is not. When someone says the database is slow, the first question is whether anything is actually slow inside it, or whether everything is simply waiting to get in."],
D("Connection pooling — reuse open connections rather than opening new ones",{say:"Opening a connection costs more than the query it carries, so every client keeps a set of them open and borrows one per query. I would size that set from what the database is comfortable handling, not from how many web servers happen to exist.",
use:["Application servers talking to a database, always.","Any repeated calls to the same destination."],
avoid:["Skipping it for databases. But do put a limit on it — unlimited is its own failure."],
consider:["Too few connections and requests queue up waiting for one while the database sits idle.","Too many and the database spends its time switching between them instead of working. Fifty servers holding fifty connections each is two and a half thousand arriving at a machine that was happiest with two hundred.","Once you have more than a couple of dozen application servers, put a shared pooler in front of the database so the total stays bounded regardless of how many servers you run."],
fails:"One slow query holds its connections. The pool empties. Requests that would have taken a millisecond now wait for a connection that does not exist, and time out. Every database dashboard looks healthy, because the database really is fine — it is the queue in front of it that is not."}),
D("Reverse proxy — the single front door in front of everything",{say:"Clients see one address. Behind it we decrypt traffic, spread requests across servers, cache what we can, enforce rate limits, and keep the internal layout private so servers can come and go freely.",
use:["The public edge of essentially every system."],
avoid:["Confusing it with the other kind of proxy. This one sits in front of your servers and acts on their behalf; a forward proxy sits in front of users and acts on theirs."],
consider:["It can answer from its own cache, so a popular redirect never reaches an application server at all.","It can reject requests over the rate limit before they cost you any application capacity.","It decides which servers are in rotation by health-checking them — and Chapter 07 covers how that decision can go badly wrong."]}),
D("Partial failure — every network call has four possible outcomes",{say:"Every arrow in this diagram can lose its response. For each one I want to be able to say what the caller does when it hears nothing back.",
use:["Apply this to every network call you draw, not just the ones that look risky."],
consider:["The four outcomes: it worked and you know; it failed and you know; it worked but the response was lost, so you believe it failed; or it is still running when you give up waiting.","A network partition is when both sides are healthy but cannot reach each other, and each concludes the other has died. Both keep acting on that belief.","Timeouts, retries, safe repetition and agreement protocols all exist because of these two facts. Chapters 10 and 11 are largely about them."],
cases:["The cache lookup for a redirect times out. Do you fall through to the database, or fail immediately to protect it? Decide in advance.","A create-link response is lost, so the client retries. A key attached to the request means it gets the same code back instead of a second one."]}),
["t","Latency vs bandwidth: a satellite link has huge bandwidth and terrible latency. The shortener is all latency — a few hundred bytes on the user's critical path — so every decision optimises round trips, not throughput."]
]},
{id:"concurrency",title:"Processes, threads and I/O models",blocks:[
["p","Two requests arrive in the same millisecond and both generate the short code a7Gk2Qp. What happens?"],
["p","The tempting answer is to coordinate: keep a set of issued codes in memory, check it before writing, and refuse the second one. It is tempting, it is wrong, and understanding exactly why is worth more than the fix itself."],
["p","It is wrong because that set lives in one process\'s memory, and you have five API servers. Each holds its own set. Each believes it. None of them knows anything about the other four. You have not prevented the collision — you have made it invisible, which is worse, because now it produces a wrong answer instead of an error."],
["p","The fix is to stop coordinating and let the database refuse. Put a unique constraint on the code column, insert, and handle the failure. One of the two requests gets a constraint violation, generates a fresh code and retries. No lock, no shared memory, no agreement between servers — the invariant is enforced in the one place all five servers already trust."],
["p","Hold on to the shape of that move, because you are going to make it repeatedly. It is the same move as the conditional inventory decrement in Chapter 04, the seat reservation in Chapter 18, and the idempotency key in Chapter 06. When several actors have to agree about something, do not build agreement between them. Push the decision down to the single component they all already talk to, and let it say no."],
["h","CPU-bound and I/O-bound want opposite things"],
["p","A redirect handler computes almost nothing. It looks up a code and writes a header. Essentially all of its wall-clock time is spent waiting — on the cache, on the database, on the network. It is I/O-bound."],
["p","Give that work a thread per request and most of your threads are asleep, while the operating system burns real CPU switching between sleeping threads. Give it an event loop and a single thread holds thousands of waiting requests, waking only when data actually arrives."],
["p","Now put an image resizer on that same event loop. It computes for two hundred milliseconds without ever yielding, and for those two hundred milliseconds every other connection on the loop is frozen. The event loop is not slow. It is single-threaded by design, and CPU-bound work violates the design."],
["p","So: I/O-bound work wants concurrency, which means more requests in flight per core. CPU-bound work wants parallelism, which means more cores. Those are different needs with different remedies, and putting both on one pool guarantees neither gets what it wanted."],
D("Threads sharing memory — cheap to coordinate, easy to corrupt",{say:"Threads share memory, so passing data between them is nearly free — and that is exactly what creates the problem. Anything shared is either never modified, protected by a lock, or handed to the database to settle.",
use:["Doing genuine computation in parallel within one process.","Handling requests in servers that give each request its own thread."],
avoid:["Thousands of threads that are mostly waiting on the network, because the operating system burns real processing time switching between sleeping threads.","Keeping anything correctness-critical in one process's memory when you run several servers. Each has its own copy and none of them knows about the others."],
consider:["A race happens when two threads read the same value, both decide based on what they read, and both write. The fix is an operation that reads and writes as one step.","A deadlock happens when each thread holds what the other needs. Always take locks in the same order and it cannot occur.","Separate processes contain a crash; threads in one process do not."],
cases:["Two requests generate the same short code at the same instant. Do not try to coordinate in memory — insert with a uniqueness rule and let one of them be rejected and try again."]}),
D("Event-loop I/O — one thread holding thousands of waiting requests",{say:"This work is nearly all waiting on the database rather than computing, so a single loop can hold thousands of requests in flight at once instead of dedicating a thread to each one that then sits idle.",
use:["Services whose time goes on waiting: APIs, proxies, gateways, and the servers holding chat connections.","Anything holding many connections that are mostly idle."],
avoid:["Handlers that do heavy computation — resizing images, hashing, rendering reports — on the same loop. One long calculation freezes every other connection on it."],
consider:["This does not make a single request faster. The database still takes exactly as long. It lets one machine handle far more requests at the same time.","Move computation-heavy work onto a separate pool of threads or into a queue, so it cannot block the loop."],
nuance:"Work limited by computation wants more cores. Work limited by waiting wants more requests in flight per core. They are different needs, and sharing one pool between them means neither gets what it needed."}),
["ex","Latency ladder — the numbers behind every 'put it in a cache'\nL1 cache         1 ns\nMemory         100 ns\nSSD read       100 µs\nDC round trip  500 µs\nDisk seek       10 ms\nUS coast↔coast  70 ms\nUS↔Europe      150 ms\n\nShortener consequence: a billion links × ~100 B fits in ~100 GB —\nthe whole mapping lives in memory; the DB is the durable copy."]
]},
{id:"performance-vocabulary",title:"Performance vocabulary as requirements",blocks:[
["p","Every word in this section is one you have already heard. The purpose of the section is to stop using them as vocabulary and start using them as requirements, and a requirement is a number attached to a consequence."],
["h","The mean is the wrong number"],
["p","Measure your redirect endpoint for an hour and find an average response time of twelve milliseconds. That sounds healthy. It tells you almost nothing."],
["p","The average is dragged around by outliers and hides the shape of the distribution entirely. Worse, no user experiences the average. Users experience their own request, and the ones deciding whether to come back are the ones on the slow end of it."],
["p","So sort the responses and look at positions instead. The median, p50, is the request in the middle: half your users had a better time than that. The p99 is the request ninety-nine percent of the way up the sorted list: one request in a hundred was worse."],
["p","Set targets on those, and notice the uncomfortable part. The slow requests are rarely random. The account with the most links has the most rows to scan. The customer with the longest history has the largest response to assemble. Your p99 is disproportionately your best users, which is precisely the wrong group to be serving worst."],
["h","Tail latency amplifies"],
["p","Now the arithmetic that makes the tail worse than it first appears."],
["p","Suppose one page load makes fifty backend calls, and each backend has a perfectly respectable one-in-a-hundred chance of being slow. What fraction of page loads contain at least one slow call?"],
["p","Not one percent. It is one minus 0.99 to the fiftieth power — about forty percent."],
["p","Two page loads in five hit somebody\'s p99. This is why a product can feel sluggish while every individual service dashboard looks fine, and why the p99 of your slowest dependency quietly becomes the p50 of your product. It is also a concrete argument against fanning a request out to more services than it needs."],
["h","Availability and durability are different promises"],
["p","A shortener that accepts a link, returns a code, and then loses the row has been perfectly available and has failed completely."],
["p","Availability is whether the system answers. Durability is whether what it already told you stays true. They fail independently, they are bought with different mechanisms — redundancy for one, replication and a durable write for the other — and a requirement that does not say which it means is not yet a requirement."],
["tbl",{cols:["Term","State it as","Consequence for the shortener"],rows:[
["Latency","p99 redirect < 20 ms at the edge","Redirects from cache/CDN; DB only on miss"],
["Throughput","50k redirects/s peak, 500 creates/s","Reads scale horizontally via cache; writes fit one leader"],
["Availability","Redirects 99.99%, creates 99.9%","Read and write paths fail independently"],
["Durability","An issued code never changes or disappears","Ack after replication; codes immutable"],
["Scalability","10× traffic without redesign","Stateless API tier; cache tier grows by node"],
["Read/write ratio","100:1","Invest in caches, not partitioning"],
["Percentiles","p50 5 ms, p99 20 ms","Tail comes from cache misses and GC — measure them, not the mean"]]}],
D("Percentiles, not averages — measure the slow requests",{say:"I would set the target on the 99th percentile rather than the average, because a page that calls fifty services hits somebody's slowest case on roughly forty percent of loads.",
use:["Every latency requirement and every alert you write."],
avoid:["Quoting an average response time in an interview. It hides the shape of the distribution and describes nobody's actual experience."],
consider:["Sort every response by how long it took. The middle one is the median. The one ninety-nine percent of the way up is the 99th percentile, meaning one request in a hundred was worse than that.","With fifty backend calls each having a one-in-a-hundred chance of being slow, about forty percent of page loads contain at least one slow call. Slowness compounds across fan-out.","The slow requests are rarely random. The user with the most data has the most to scan, so your worst experiences tend to belong to your most engaged users.","Common causes of a slow tail: a cache miss, a garbage collection pause, waiting on a lock, a connection being opened for the first time, or a slow disk.","Improving the average is easy. Improving the tail is the actual work."]}),
["t","Availability and durability are different promises: a shortener that acknowledges a link and then forgets it has failed durability while being perfectly available. Say which one each requirement is about."]
]}
]};
